---
phase: 04-autonomous-repo-execution-and-validation-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - maf_starter/validation_runner.py
  - maf_starter/orchestration.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_store.py
  - autogen_dashboard/session_runner.py
  - tests/test_phase4_validation.py
  - tests/test_run_persistence.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
user_setup: []
must_haves:
  truths:
    - "The system runs targeted local validation commands chosen from repo context, changed files, and stack hints."
    - "Validation results are persisted with command, cwd, exit code, duration, and output summaries under the run artifacts."
    - "Validation failures never disappear into transcript text and instead pause the run with actionable recorded results."
  artifacts:
    - path: "maf_starter/validation_runner.py"
      provides: "Shared targeted-validation ladder with safe command selection and result recording"
      min_lines: 180
      contains: "ValidationResult"
    - path: "autogen_dashboard/session_store.py"
      provides: "Durable validation artifact paths and manifest entries under the validation stage"
      min_lines: 300
      contains: "validation"
    - path: "tests/test_phase4_validation.py"
      provides: "Regression coverage for validation planning, command execution, and failure handling"
      min_lines: 140
      contains: "ValidationResult"
  key_links:
    - from: "autogen_dashboard/session_runner.py"
      to: "maf_starter/validation_runner.py"
      via: "manager-owned validation-stage execution and result persistence"
      pattern: "validation|command|result"
    - from: "maf_starter/validation_runner.py"
      to: "autogen_dashboard/session_store.py"
      via: "validation artifact records and manifest wiring"
      pattern: "validation|artifacts|results"
---

<objective>
Add targeted validation runners and durable result recording on top of the change-capture contract.

Purpose: Autonomous repo editing is not trustworthy until the runtime can run bounded local validation, capture what it ran, and pause visibly when validation fails.
Output: A shared validation runner, durable validation artifacts, and regression coverage for validation selection and failure handling.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-CONTEXT.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-RESEARCH.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-VALIDATION.md
@maf_starter/orchestration.py
@autogen_dashboard/session_store.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
@tests/test_phase2_runtime.py
@tests/test_run_persistence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a targeted validation runner with a safe command ladder</name>
  <files>maf_starter/validation_runner.py</files>
  <read_first>maf_starter/orchestration.py, .planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-RESEARCH.md, .planning/codebase/TESTING.md, README.md</read_first>
  <action>Create `maf_starter/validation_runner.py` with serializable `ValidationCommand`, `ValidationPlan`, and `ValidationResult` types plus helpers to select and run safe local commands. The first ladder must use concrete command families: `git diff --check` for git-backed workspaces, `python -m compileall ...` for Python changes, `python -m unittest discover -s tests -v` when Python changes touch runtime or test paths and a `tests/` directory exists, and `node --check <file>` for changed JavaScript files. Only select commands that run under the selected repo root and do not install packages or call external services.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation -v</verify>
  <acceptance_criteria>
    - `maf_starter/validation_runner.py` contains `ValidationCommand`
    - `maf_starter/validation_runner.py` contains `ValidationPlan`
    - `maf_starter/validation_runner.py` contains `ValidationResult`
    - `maf_starter/validation_runner.py` contains `git diff --check`
    - `maf_starter/validation_runner.py` contains `python -m unittest discover -s tests -v`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation -v` exits with code 0
  </acceptance_criteria>
  <done>The runtime can derive a safe, targeted validation plan from changed files and repo context.</done>
</task>

<task type="auto">
  <name>Task 2: Persist validation plans and results under the validation stage and pause on failure</name>
  <files>maf_starter/orchestration.py, autogen_dashboard/schemas.py, autogen_dashboard/session_store.py, autogen_dashboard/session_runner.py, maf_starter/validation_runner.py</files>
  <read_first>maf_starter/validation_runner.py, maf_starter/orchestration.py, autogen_dashboard/session_store.py, autogen_dashboard/schemas.py, autogen_dashboard/session_runner.py</read_first>
  <action>Extend the validation-stage runtime so `autogen_dashboard/session_runner.py` calls the new validation runner after implementation artifacts exist, persists the validation plan and results, and converts failed validation into a `retryable_error` or `blocked` pause instead of silently completing the run. Add explicit artifact paths in `autogen_dashboard/session_store.py` for `artifacts/stages/validation/commands.json` and `artifacts/stages/validation/results.json`, and extend `autogen_dashboard/schemas.py` so session and stage payloads expose validation commands, validation results, and validation summary fields directly.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation tests.test_run_persistence -v</verify>
  <acceptance_criteria>
    - `autogen_dashboard/session_store.py` contains `commands.json`
    - `autogen_dashboard/session_store.py` contains `results.json`
    - `autogen_dashboard/schemas.py` contains `validation_results`
    - `autogen_dashboard/session_runner.py` contains `retryable_error`
    - `autogen_dashboard/session_runner.py` contains `validation`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation tests.test_run_persistence -v` exits with code 0
  </acceptance_criteria>
  <done>Validation is now a durable stage with machine-readable commands, results, and failure-to-pause behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for validation planning, execution, and recorded failures</name>
  <files>tests/test_phase4_validation.py, tests/test_run_persistence.py, tests/test_phase2_runtime.py</files>
  <read_first>maf_starter/validation_runner.py, autogen_dashboard/session_runner.py, tests/test_run_persistence.py, tests/test_phase2_runtime.py</read_first>
  <action>Create `tests/test_phase4_validation.py` to verify command selection from changed files, cwd confinement, result recording, and failure classification. Extend `tests/test_run_persistence.py` so the artifact manifest hydrates validation commands and results, and add a focused runtime regression to `tests/test_phase2_runtime.py` or a comparable runtime suite proving failed validation pauses the run instead of marking it complete.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation tests.test_run_persistence tests.test_phase2_runtime -v</verify>
  <acceptance_criteria>
    - `tests/test_phase4_validation.py` contains `ValidationPlan`
    - `tests/test_phase4_validation.py` contains `ValidationResult`
    - `tests/test_run_persistence.py` contains `commands.json`
    - `tests/test_run_persistence.py` contains `results.json`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation tests.test_run_persistence tests.test_phase2_runtime -v` exits with code 0
  </acceptance_criteria>
  <done>The targeted-validation contract is protected by repeatable runtime and persistence checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_validation tests.test_run_persistence tests.test_phase2_runtime -v`
- [ ] Validation commands are selected from safe built-in rules and run only under the selected repo root
- [ ] Validation failures pause the run with recorded results instead of silently completing
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `EXEC-03` and the validation-result core of `EXEC-02` are satisfied in the active runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-02-SUMMARY.md`
</output>
