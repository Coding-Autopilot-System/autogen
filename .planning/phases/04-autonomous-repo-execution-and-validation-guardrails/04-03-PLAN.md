---
phase: 04-autonomous-repo-execution-and-validation-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - maf_core/approval_policy.py
  - maf_core/tools.py
  - maf_core/repo_execution.py
  - maf_core/validation_runner.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_runner.py
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/app.js
  - autogen_dashboard/static/styles.css
  - tests/test_phase4_approval.py
  - tests/test_phase3_api.py
autonomous: true
requirements:
  - EXEC-04
  - EXEC-01
  - EXEC-03
user_setup: []
must_haves:
  truths:
    - "Destructive or externally visible actions always trigger an explicit approval step before execution."
    - "Routine safe repo edits and local validation continue automatically without per-step approval."
    - "Pending approval payloads name the action, scope, reason, and affected paths or resources in operator-visible state."
  artifacts:
    - path: "maf_core/approval_policy.py"
      provides: "Central approval classifier for routine-safe, destructive, externally-visible, and blocked actions"
      min_lines: 160
      contains: "ApprovalScope"
    - path: "autogen_dashboard/static/app.js"
      provides: "Operator-visible approval scope cards and queue rendering for risky actions"
      min_lines: 2000
      contains: "approval"
    - path: "tests/test_phase4_approval.py"
      provides: "Regression coverage for approval classification, pending scope payloads, and pause behavior"
      min_lines: 140
      contains: "ApprovalScope"
  key_links:
    - from: "autogen_dashboard/session_runner.py"
      to: "maf_core/approval_policy.py"
      via: "risk classification before write and validation actions execute"
      pattern: "approval|risk|scope"
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/schemas.py"
      via: "operator rendering of pending approval scope and explicit reason"
      pattern: "approval|scope|affected"
---

<objective>
Enforce explicit approval policy for destructive or externally visible actions and expose that scope clearly to the operator.

Purpose: Phase 4 is only safe when approval behavior is policy-driven and visible, not dependent on prompt wording or ad hoc tool calls.
Output: A shared approval-classification module, runtime pauses for risky actions, operator-facing approval-scope cards, and regression coverage for approval behavior.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-CONTEXT.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-RESEARCH.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-VALIDATION.md
@maf_core/tools.py
@maf_core/repo_execution.py
@maf_core/validation_runner.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
@autogen_dashboard/static/index.html
@autogen_dashboard/static/app.js
@autogen_dashboard/static/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a central approval classifier for risky actions</name>
  <files>maf_core/approval_policy.py, maf_core/repo_execution.py, maf_core/validation_runner.py, maf_core/tools.py</files>
  <read_first>maf_core/repo_execution.py, maf_core/validation_runner.py, maf_core/tools.py, .planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-CONTEXT.md, .planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-RESEARCH.md</read_first>
  <action>Create `maf_core/approval_policy.py` with concrete serializable types named `ApprovalScope` and `ExecutionRiskDecision`, plus helpers that classify actions as `routine_safe`, `destructive`, `externally_visible`, or `blocked`. Treat `create_file`, `update_file`, `append_file`, `git diff --check`, `python -m compileall ...`, `python -m unittest ...`, and `node --check ...` as routine safe when they stay inside the selected repo root. Treat file deletes, file moves, writes outside the repo, `.env` mutations, `git reset`, `git clean`, `git checkout`, `git commit`, `git push`, install commands, deploy commands, notification sends, and service start or stop commands as approval-required or blocked. Update the write and validation modules so they surface enough action metadata for this classifier to decide before execution.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_approval -v</verify>
  <acceptance_criteria>
    - `maf_core/approval_policy.py` contains `ApprovalScope`
    - `maf_core/approval_policy.py` contains `ExecutionRiskDecision`
    - `maf_core/approval_policy.py` contains `routine_safe`
    - `maf_core/approval_policy.py` contains `externally_visible`
    - `maf_core/approval_policy.py` contains `destructive`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_approval -v` exits with code 0
  </acceptance_criteria>
  <done>The runtime now has one explicit policy module for deciding when approval is required.</done>
</task>

<task type="auto">
  <name>Task 2: Pause risky actions with explicit approval scope and expose that payload in the operator surface</name>
  <files>autogen_dashboard/schemas.py, autogen_dashboard/session_runner.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/schemas.py, autogen_dashboard/session_runner.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, maf_core/approval_policy.py</read_first>
  <action>Extend `autogen_dashboard/schemas.py` with a structured pending-approval payload that includes `action_kind`, `risk_level`, `reason`, `affected_paths`, `commands`, and `external_targets`. Update `autogen_dashboard/session_runner.py` so implementation and validation stages classify each action before execution, pause the run with `needs_approval` when the decision is not `routine_safe`, and persist the exact pending approval scope in the session summary and detail payloads. Update the operator surface so `Overview` and the approval queue render a clear approval-scope card showing the action, scope, reason, and what approval will unblock.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/schemas.py` contains `affected_paths`
    - `autogen_dashboard/schemas.py` contains `risk_level`
    - `autogen_dashboard/session_runner.py` contains `needs_approval`
    - `autogen_dashboard/session_runner.py` contains `pending_approval`
    - `autogen_dashboard/static/index.html` contains `approval`
    - `autogen_dashboard/static/app.js` contains `affectedPaths`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>Approval-required actions now stop with explicit operator-visible scope instead of hidden or ad hoc approval behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for approval classification, pause behavior, and payload exposure</name>
  <files>tests/test_phase4_approval.py, tests/test_phase3_api.py</files>
  <read_first>maf_core/approval_policy.py, autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py, tests/test_phase3_api.py</read_first>
  <action>Create `tests/test_phase4_approval.py` to verify classification of routine-safe versus destructive and externally-visible actions, pending approval payload contents, and paused-run behavior before execution. Extend `tests/test_phase3_api.py` or a comparable API contract suite so session payloads expose the new approval-scope fields without breaking prior routing and specialist visibility coverage. End the plan by running the new approval tests, the API contract tests, the full suite, compile smoke, and `node --check autogen_dashboard\\static\\app.js`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_approval tests.test_phase3_api -v</verify>
  <acceptance_criteria>
    - `tests/test_phase4_approval.py` contains `ApprovalScope`
    - `tests/test_phase4_approval.py` contains `externally_visible`
    - `tests/test_phase3_api.py` contains `affected_paths`
    - `tests/test_phase3_api.py` contains `risk_level`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_approval tests.test_phase3_api -v` exits with code 0
  </acceptance_criteria>
  <done>The approval-policy contract is protected by runtime and API regression checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_approval tests.test_phase3_api -v`
- [ ] `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`
- [ ] `.\.venv\Scripts\python.exe -m compileall maf_core autogen_dashboard tests main.py`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] Destructive or externally visible actions always pause with explicit approval scope before execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `EXEC-04` is satisfied while routine safe edits and validation remain autonomous
  </success_criteria>

<output>
After completion, create `.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-03-SUMMARY.md`
</output>
