---
phase: 04-autonomous-repo-execution-and-validation-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - maf_core/repo_execution.py
  - maf_core/tools.py
  - maf_core/orchestration.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_store.py
  - autogen_dashboard/session_runner.py
  - tests/test_phase4_write_execution.py
  - tests/test_run_persistence.py
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
user_setup: []
must_haves:
  truths:
    - "Autonomous runs can apply routine safe file edits inside the selected repo or worktree without per-step approval."
    - "Each implementation-capable run persists changed-file lists, per-file operation records, and a unified diff artifact."
    - "Write execution remains bounded to the selected repo root and explicitly denies secret-bearing and out-of-scope paths."
  artifacts:
    - path: "maf_core/repo_execution.py"
      provides: "Shared controlled write-execution service with structured operation records and diff capture"
      min_lines: 180
      contains: "WriteOperationRecord"
    - path: "autogen_dashboard/session_store.py"
      provides: "Durable change-artifact manifest entries and per-stage change artifact paths"
      min_lines: 280
      contains: "manifest"
    - path: "tests/test_phase4_write_execution.py"
      provides: "Regression coverage for path safety, write operations, and change-capture artifacts"
      min_lines: 140
      contains: "WriteOperationRecord"
  key_links:
    - from: "autogen_dashboard/session_runner.py"
      to: "maf_core/repo_execution.py"
      via: "manager-owned implementation-stage write execution and artifact capture"
      pattern: "write|diff|operation"
    - from: "autogen_dashboard/session_store.py"
      to: "autogen_dashboard/schemas.py"
      via: "change artifact manifest and run payload hydration"
      pattern: "artifact|diff|changed_files"
---

<objective>
Add controlled write execution and change capture on top of the existing manager-owned run contract.

Purpose: Phase 4 cannot become a safe default-doer until the runtime can apply bounded repo edits, capture what changed, and persist those artifacts without relying on transcript text.
Output: A shared write-execution service, durable change artifacts under the existing run manifest, and regression coverage for path safety and diff capture.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-CONTEXT.md
@.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-RESEARCH.md
@maf_core/tools.py
@maf_core/orchestration.py
@autogen_dashboard/session_store.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
@tests/test_run_persistence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a controlled repo write-execution service with explicit safe operations</name>
  <files>maf_core/repo_execution.py, maf_core/tools.py</files>
  <read_first>maf_core/tools.py, .planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-CONTEXT.md, .planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-RESEARCH.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Create `maf_core/repo_execution.py` and move routine-safe write behavior behind explicit helpers instead of raw shell. Add serializable types named `WriteOperationRecord` and `ChangeCaptureResult`, plus concrete helpers to `create_file`, `update_file`, and `append_file` under the selected repo root. Reject deletes, renames, writes outside the repo root, `.git/**`, `.venv/**`, `state/**`, `.env`, and `.env.*` in this plan. Update `maf_core/tools.py` so the repo tool boundary can call the new service and reuse the same root-resolution and denial rules for future write-capable paths.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution -v</verify>
  <acceptance_criteria>
    - `maf_core/repo_execution.py` contains `WriteOperationRecord`
    - `maf_core/repo_execution.py` contains `ChangeCaptureResult`
    - `maf_core/repo_execution.py` contains `create_file`
    - `maf_core/repo_execution.py` contains `update_file`
    - `maf_core/repo_execution.py` contains `append_file`
    - `maf_core/tools.py` contains `request_human_approval`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution -v` exits with code 0
  </acceptance_criteria>
  <done>The runtime has one shared, bounded write-execution service for routine safe repo edits.</done>
</task>

<task type="auto">
  <name>Task 2: Persist changed-file lists, operation records, and unified diffs under the run manifest</name>
  <files>maf_core/repo_execution.py, maf_core/orchestration.py, autogen_dashboard/schemas.py, autogen_dashboard/session_store.py, autogen_dashboard/session_runner.py</files>
  <read_first>maf_core/repo_execution.py, maf_core/orchestration.py, autogen_dashboard/session_store.py, autogen_dashboard/schemas.py, autogen_dashboard/session_runner.py</read_first>
  <action>Extend the durable run contract so implementation-stage writes persist `changed_files`, `write_operations`, and `diff_artifacts` under the existing stage artifact tree. Add explicit artifact paths in `autogen_dashboard/session_store.py` for `artifacts/stages/implementation/changes/files.json`, `artifacts/stages/implementation/changes/operations.json`, and `artifacts/stages/implementation/changes/diff.patch`. Update `autogen_dashboard/schemas.py` and `autogen_dashboard/session_runner.py` so session detail and stage outputs expose those fields directly, and have the implementation-stage flow call the new write service and attach the returned artifact paths and summaries.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution tests.test_run_persistence -v</verify>
  <acceptance_criteria>
    - `autogen_dashboard/session_store.py` contains `diff.patch`
    - `autogen_dashboard/session_store.py` contains `operations.json`
    - `autogen_dashboard/schemas.py` contains `changed_files`
    - `autogen_dashboard/schemas.py` contains `diff_artifacts`
    - `autogen_dashboard/session_runner.py` contains `write_operations`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution tests.test_run_persistence -v` exits with code 0
  </acceptance_criteria>
  <done>Implementation-stage changes are now durable, inspectable run artifacts instead of transcript-only summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for write safety, change capture, and manifest hydration</name>
  <files>tests/test_phase4_write_execution.py, tests/test_run_persistence.py</files>
  <read_first>tests/test_run_persistence.py, maf_core/repo_execution.py, autogen_dashboard/session_store.py, autogen_dashboard/session_runner.py</read_first>
  <action>Create `tests/test_phase4_write_execution.py` to verify repo-root confinement, secret-path denial, routine-safe create/update/append behavior, changed-file detection, and unified diff capture. Extend `tests/test_run_persistence.py` so artifact-manifest hydration covers the new implementation change artifacts and stage-level change payloads. Keep the tests filesystem-local under scratch directories and do not depend on network or installed external services.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution tests.test_run_persistence -v</verify>
  <acceptance_criteria>
    - `tests/test_phase4_write_execution.py` contains `ChangeCaptureResult`
    - `tests/test_phase4_write_execution.py` contains `diff.patch`
    - `tests/test_run_persistence.py` contains `operations.json`
    - `tests/test_run_persistence.py` contains `diff.patch`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution tests.test_run_persistence -v` exits with code 0
  </acceptance_criteria>
  <done>The write-execution and change-capture contract is protected by repeatable automated checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase4_write_execution tests.test_run_persistence -v`
- [ ] Routine safe create, update, and append operations stay inside the selected repo root
- [ ] Changed-file lists, write-operation records, and a unified diff artifact are persisted under the run manifest
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `EXEC-01` and the change-artifact core of `EXEC-02` are satisfied in the active runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/04-autonomous-repo-execution-and-validation-guardrails/04-01-SUMMARY.md`
</output>
