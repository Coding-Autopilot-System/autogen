---
phase: 03-specialist-delegation-and-routing-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - maf_core/config.py
  - maf_core/routing_policy.py
  - maf_core/routing_types.py
  - maf_core/provider_fallback.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_runner.py
  - tests/test_phase3_routing.py
  - tests/test_maf_setup.py
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
user_setup: []
must_haves:
  truths:
    - "The operator can choose a preferred model or route lane before the run starts."
    - "Each routed turn records the planned lane, actual provider/model, route rationale, and fallback chain outcome."
    - "Fallback events show whether capability changed, especially when tools are no longer available."
  artifacts:
    - path: "maf_core/routing_policy.py"
      provides: "Explicit route-lane selection and lane-to-chain mapping for the active runtime"
      min_lines: 120
      contains: "lane"
    - path: "maf_core/provider_fallback.py"
      provides: "Full route attempt metadata, fallback recording, and capability-drift reporting"
      min_lines: 320
      contains: "tools_available"
    - path: "tests/test_phase3_routing.py"
      provides: "Regression coverage for lane selection, route metadata, fallback attempts, and capability changes"
      min_lines: 120
      contains: "route"
  key_links:
    - from: "maf_core/provider_fallback.py"
      to: "maf_core/routing_policy.py"
      via: "planned route versus actual route metadata"
      pattern: "route|lane|fallback"
    - from: "autogen_dashboard/session_runner.py"
      to: "autogen_dashboard/schemas.py"
      via: "route plan and route outcome projection"
      pattern: "route_metadata|route_lane|fallback"
---

<objective>
Build explicit route-lane selection, route metadata, and fallback capability reporting for the active runtime.

Purpose: Specialist visibility is incomplete if the operator still cannot tell what route was intended, what route actually executed, and whether fallback changed what the system could safely do.
Output: Lane-first routing controls in the runtime contract, richer route metadata, and regression tests for route visibility and capability drift.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md
@.planning/phases/03-specialist-delegation-and-routing-visibility/03-RESEARCH.md
@maf_core/config.py
@maf_core/routing_policy.py
@maf_core/routing_types.py
@maf_core/provider_fallback.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize operator-facing route lanes and model pinning in the runtime contract</name>
  <files>maf_core/config.py, maf_core/routing_policy.py, maf_core/routing_types.py, autogen_dashboard/schemas.py</files>
  <read_first>maf_core/config.py, maf_core/routing_policy.py, maf_core/routing_types.py, autogen_dashboard/schemas.py, .planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md</read_first>
  <action>Extend the runtime contract so route choice is lane-first. Add explicit lane identifiers such as `auto`, `deep`, `balanced`, and `fast` to the routing types or policy layer, and add any missing config or schema fields needed to persist `route_lane`, `route_plan`, `requested_provider`, and `requested_model`. Update `maf_core/routing_policy.py` so lane choice is explicit in the returned routing plan rather than inferred only from `simple`, `standard`, and `deep` internals. Keep advanced model pinning available by preserving a concrete requested model or provider override field in the session schema.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing -v</verify>
  <acceptance_criteria>
    - `maf_core/routing_policy.py` contains `auto`
    - `maf_core/routing_policy.py` contains `balanced`
    - `autogen_dashboard/schemas.py` contains `route_lane`
    - `autogen_dashboard/schemas.py` contains `requested_model`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing -v` exits with code 0
  </acceptance_criteria>
  <done>The active runtime has an explicit lane-first route contract with advanced model pinning support.</done>
</task>

<task type="auto">
  <name>Task 2: Record full route outcomes, fallback attempts, and capability drift</name>
  <files>maf_core/provider_fallback.py, autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py</files>
  <read_first>maf_core/provider_fallback.py, autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py, maf_core/routing_policy.py</read_first>
  <action>Update `maf_core/provider_fallback.py` so each routed turn records the requested lane, the planned primary provider and model, the attempted fallback steps in order, the actual provider and model used, and explicit capability-change information when fallback alters tool availability. Update `autogen_dashboard/session_runner.py` to persist that route plan and route outcome data per run or attempt and project it into the session summary. Extend `autogen_dashboard/schemas.py` so route data includes `route_lane`, `route_plan`, `route_attempts`, `fallback_count`, `fallback_used`, `tools_available`, and an explicit `capability_changes` list or equivalent concrete structure.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing tests.test_maf_setup -v</verify>
  <acceptance_criteria>
    - `maf_core/provider_fallback.py` contains `route_attempts`
    - `maf_core/provider_fallback.py` contains `tools_available`
    - `autogen_dashboard/session_runner.py` contains `route_lane`
    - `autogen_dashboard/schemas.py` contains `capability_changes`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing tests.test_maf_setup -v` exits with code 0
  </acceptance_criteria>
  <done>The runtime and durable run model now record the difference between the planned route and the actual route outcome, including capability drift.</done>
</task>

<task type="auto">
  <name>Task 3: Add routing regression coverage and preserve fallback smoke behavior</name>
  <files>tests/test_phase3_routing.py, tests/test_maf_setup.py</files>
  <read_first>tests/test_maf_setup.py, maf_core/routing_policy.py, maf_core/provider_fallback.py, autogen_dashboard/session_runner.py</read_first>
  <action>Create `tests/test_phase3_routing.py` to verify lane selection, planned route ordering, requested-model overrides, fallback attempt capture, and capability-drift metadata when API-to-CLI fallback occurs. Extend `tests/test_maf_setup.py` with smoke assertions that the active runtime still emits route metadata fields needed by the operator surface after the new lane-first contract lands.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing tests.test_maf_setup -v</verify>
  <acceptance_criteria>
    - `tests/test_phase3_routing.py` contains `route_lane`
    - `tests/test_phase3_routing.py` contains `capability`
    - `tests/test_maf_setup.py` contains `tools_available`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing tests.test_maf_setup -v` exits with code 0
  </acceptance_criteria>
  <done>The route-lane and fallback-visibility contract is covered by repeatable tests and runtime smoke checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_routing tests.test_maf_setup -v`
- [ ] The runtime persists both the planned route and the actual route outcome
- [ ] Capability drift is explicit when fallback removes tool support or changes execution mode
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ROUT-01`, `ROUT-02`, and `ROUT-03` are satisfied at the runtime and durable-state level
  </success_criteria>

<output>
After completion, create `.planning/phases/03-specialist-delegation-and-routing-visibility/03-02-SUMMARY.md`
</output>
