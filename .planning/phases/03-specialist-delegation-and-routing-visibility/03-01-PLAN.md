---
phase: 03-specialist-delegation-and-routing-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - maf_starter/orchestration.py
  - maf_starter/team_factory.py
  - maf_starter/agent_factory.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_runner.py
  - entities/repo_team/workflow.py
  - tests/test_phase3_specialists.py
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
user_setup: []
must_haves:
  truths:
    - "The operator can see which specialist agents are participating in the run and what role each one owns."
    - "Each specialist exposes current task, latest output, and handoff status in structured run data."
    - "The manager delegates work to specialists without the operator manually prompt-choreographing each step."
  artifacts:
    - path: "maf_starter/orchestration.py"
      provides: "Shared specialist-state, handoff, and stage-ownership contract layered under the manager-run state"
      min_lines: 180
      contains: "SpecialistState"
    - path: "autogen_dashboard/schemas.py"
      provides: "Persisted specialist roster, specialist state, and handoff payload contract for the operator surface"
      min_lines: 180
      contains: "specialist"
    - path: "tests/test_phase3_specialists.py"
      provides: "Regression coverage for specialist roster, ownership, handoffs, and idle/running visibility"
      min_lines: 120
      contains: "handoff"
  key_links:
    - from: "maf_starter/team_factory.py"
      to: "maf_starter/orchestration.py"
      via: "shared specialist roster and stage ownership"
      pattern: "planner|researcher|implementer|reviewer|manager"
    - from: "autogen_dashboard/session_runner.py"
      to: "autogen_dashboard/schemas.py"
      via: "specialist-state and handoff projection"
      pattern: "specialist|handoff"
---

<objective>
Formalize specialist roles, handoffs, and per-agent state on top of the manager-owned run contract.

Purpose: Phase 3 cannot deliver useful specialist visibility until the runtime knows who the participants are, what each one owns, and how work moves between them.
Output: A shared specialist-state contract, a manager workflow that emits explicit specialist ownership and handoffs, and regression coverage for the new participant model.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md
@.planning/phases/03-specialist-delegation-and-routing-visibility/03-RESEARCH.md
@maf_starter/orchestration.py
@maf_starter/team_factory.py
@maf_starter/agent_factory.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
@entities/repo_team/workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the shared orchestration contract with specialist and handoff state</name>
  <files>maf_starter/orchestration.py, autogen_dashboard/schemas.py</files>
  <read_first>maf_starter/orchestration.py, autogen_dashboard/schemas.py, .planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md, .planning/phases/03-specialist-delegation-and-routing-visibility/03-RESEARCH.md</read_first>
  <action>Extend `maf_starter/orchestration.py` with explicit specialist-level types and helpers that sit under the existing manager stage contract: add a stable specialist roster for `manager`, `planner`, `researcher`, `implementer`, and `reviewer`; add serializable `SpecialistState` records with concrete fields `role`, `stage`, `status`, `current_task`, `latest_output_summary`, `last_handoff_target`, `last_handoff_reason`, `started_at`, `updated_at`, and `completed_at`; add serializable `SpecialistHandoff` records with `from_role`, `to_role`, `reason`, `requested_by`, `status`, and timestamps; and add helpers to initialize the full roster in a not-started state. Extend `autogen_dashboard/schemas.py` with typed Pydantic models for the same specialist and handoff payloads plus `specialist_states` and `specialist_handoffs` fields on the run summary and detail models.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists -v</verify>
  <acceptance_criteria>
    - `maf_starter/orchestration.py` contains `SpecialistState`
    - `maf_starter/orchestration.py` contains `SpecialistHandoff`
    - `maf_starter/orchestration.py` contains `planner`
    - `autogen_dashboard/schemas.py` contains `specialist_states`
    - `autogen_dashboard/schemas.py` contains `specialist_handoffs`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists -v` exits with code 0
  </acceptance_criteria>
  <done>The shared run contract can describe visible specialists and structured handoffs.</done>
</task>

<task type="auto">
  <name>Task 2: Project specialist ownership and handoffs through the active workflow runtime</name>
  <files>maf_starter/team_factory.py, maf_starter/agent_factory.py, autogen_dashboard/session_runner.py, entities/repo_team/workflow.py</files>
  <read_first>maf_starter/team_factory.py, maf_starter/agent_factory.py, autogen_dashboard/session_runner.py, entities/repo_team/workflow.py, maf_starter/orchestration.py</read_first>
  <action>Update `maf_starter/team_factory.py` so the existing `planner`, `researcher`, `implementer`, and `reviewer` participants publish specialist ownership metadata instead of only stage metadata. Keep the manager as the run owner, but require the workflow and specialist instructions to emit concrete `current_task`, `latest_output_summary`, `handoff_to`, and `handoff_reason` values. Update `autogen_dashboard/session_runner.py` so stage execution initializes the full specialist roster, marks the active specialist as running, updates the latest output summary after each stage, persists explicit handoff events between specialists, and projects that state back into the session summary. Keep `entities/repo_team/workflow.py` aligned so its description makes the visible specialist contract explicit.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists -v</verify>
  <acceptance_criteria>
    - `maf_starter/team_factory.py` contains `planner`
    - `maf_starter/team_factory.py` contains `reviewer`
    - `autogen_dashboard/session_runner.py` contains `specialist`
    - `autogen_dashboard/session_runner.py` contains `handoff`
    - `entities/repo_team/workflow.py` contains `planner`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists -v` exits with code 0
  </acceptance_criteria>
  <done>The active manager workflow now produces visible specialist ownership and handoff state under one durable run.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for specialist roster, ownership, and handoffs</name>
  <files>tests/test_phase3_specialists.py, tests/test_maf_setup.py</files>
  <read_first>tests/test_maf_setup.py, maf_starter/orchestration.py, maf_starter/team_factory.py, autogen_dashboard/session_runner.py</read_first>
  <action>Create `tests/test_phase3_specialists.py` to verify the stable specialist roster, idle state initialization, role-to-stage ownership, handoff serialization, and run-summary projection of specialist state. Extend `tests/test_maf_setup.py` with smoke assertions that the active `repo_team` workflow still builds without network access and exposes the specialist roster metadata needed by the operator surface. The tests must prove that specialist visibility is durable and does not depend on transcript scraping.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists tests.test_maf_setup -v</verify>
  <acceptance_criteria>
    - `tests/test_phase3_specialists.py` contains `SpecialistState`
    - `tests/test_phase3_specialists.py` contains `handoff`
    - `tests/test_phase3_specialists.py` contains `planner`
    - `tests/test_maf_setup.py` contains `repo_team`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists tests.test_maf_setup -v` exits with code 0
  </acceptance_criteria>
  <done>The specialist-state contract is protected by repeatable automated checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_specialists tests.test_maf_setup -v`
- [ ] The full specialist roster exists even before each participant has acted
- [ ] Specialist ownership and handoffs are persisted as structured run data rather than transcript-only cues
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `AGNT-01`, `AGNT-02`, and the delegation core of `AGNT-03` are established in the active runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/03-specialist-delegation-and-routing-visibility/03-01-SUMMARY.md`
</output>
