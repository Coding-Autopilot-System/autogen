---
phase: 03-specialist-delegation-and-routing-visibility
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - autogen_dashboard/app.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_runner.py
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/app.js
  - autogen_dashboard/static/styles.css
  - tests/test_phase3_api.py
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - ROUT-01
  - ROUT-02
  - ROUT-03
user_setup: []
must_haves:
  truths:
    - "The operator can inspect specialist participation, current task ownership, and handoff status without reading raw logs."
    - "The operator can inspect route lanes, actual provider/model use, fallback activity, and capability changes through dedicated routing surfaces."
    - "The operator surface exposes specialist and routing data through structural views instead of burying it in one transcript stream."
  artifacts:
    - path: "autogen_dashboard/static/index.html"
      provides: "Structural operator views for overview, agents, routing, and artifacts or traces"
      min_lines: 220
      contains: "Agents"
    - path: "autogen_dashboard/static/app.js"
      provides: "Client-side rendering for specialist panels, route cards, and structural operator views"
      min_lines: 1800
      contains: "route"
    - path: "tests/test_phase3_api.py"
      provides: "API and operator-payload coverage for specialist and routing visibility"
      min_lines: 120
      contains: "specialist"
  key_links:
    - from: "autogen_dashboard/app.py"
      to: "autogen_dashboard/session_runner.py"
      via: "specialist and routing payload exposure"
      pattern: "specialist|route"
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/schemas.py"
      via: "operator rendering of specialist state and route metadata"
      pattern: "specialist|routeLane|routeMetadata"
---

<objective>
Expose specialist and routing data cleanly in the operator surface.

Purpose: Phase 3 is only complete when the operator can actually use the new specialist and routing contracts from the product UI, not just through persisted JSON or DevUI traces.
Output: API payloads and operator views for overview, agents, routing, and artifacts/traces, plus coverage for the new visibility contract.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md
@.planning/phases/03-specialist-delegation-and-routing-visibility/03-RESEARCH.md
@autogen_dashboard/app.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
@autogen_dashboard/static/index.html
@autogen_dashboard/static/app.js
@autogen_dashboard/static/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose specialist and routing data through the API and SSE contracts</name>
  <files>autogen_dashboard/app.py, autogen_dashboard/schemas.py, autogen_dashboard/session_runner.py</files>
  <read_first>autogen_dashboard/app.py, autogen_dashboard/schemas.py, autogen_dashboard/session_runner.py, .planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md</read_first>
  <action>Extend the operator API and SSE payloads so session summary and detail responses expose `specialist_states`, `specialist_handoffs`, `route_lane`, `route_plan`, `route_attempts`, and `capability_changes` directly. Update `autogen_dashboard/session_runner.py` so it projects the specialist and routing state produced by Plans `03-01` and `03-02` into stable run payloads rather than leaving them buried in raw artifacts. Keep the field names explicit and aligned with the Pydantic schema contract.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase3_api -v</verify>
  <acceptance_criteria>
    - `autogen_dashboard/app.py` contains `specialist`
    - `autogen_dashboard/app.py` contains `route_lane`
    - `autogen_dashboard/session_runner.py` contains `specialist_states`
    - `autogen_dashboard/session_runner.py` contains `route_attempts`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_api -v` exits with code 0
  </acceptance_criteria>
  <done>The product API exposes specialist and routing visibility as first-class fields.</done>
</task>

<task type="auto">
  <name>Task 2: Build structural operator views for Overview, Agents, Routing, and Artifacts/Traces</name>
  <files>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, autogen_dashboard/schemas.py, .planning/phases/03-specialist-delegation-and-routing-visibility/03-CONTEXT.md</read_first>
  <action>Refactor the current operator shell so it exposes structural views for `Overview`, `Agents`, `Routing`, and `Artifacts/Traces`. In `Overview`, keep the manager summary and current stage. In `Agents`, render one rounded card or tab per specialist with role, state, current task, latest output summary, and handoff target. In `Routing`, render the selected lane, planned route chain, actual provider/model outcome, fallback attempts, and capability-change badges. In `Artifacts/Traces`, keep access to lower-level details without making them the primary path. Keep the design aligned with the current rounded-card direction, but do not spend this phase on final motion or full visual-system polish.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/index.html` contains `Overview`
    - `autogen_dashboard/static/index.html` contains `Agents`
    - `autogen_dashboard/static/index.html` contains `Routing`
    - `autogen_dashboard/static/app.js` contains `specialist`
    - `autogen_dashboard/static/app.js` contains `routeLane`
    - `autogen_dashboard/static/styles.css` contains `routing`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The operator surface can inspect specialist and routing behavior without raw log reading.</done>
</task>

<task type="auto">
  <name>Task 3: Add API and operator-surface coverage for specialist and routing visibility</name>
  <files>tests/test_phase3_api.py</files>
  <read_first>autogen_dashboard/app.py, autogen_dashboard/schemas.py, autogen_dashboard/static/app.js, tests/test_phase2_api.py</read_first>
  <action>Create `tests/test_phase3_api.py` to verify that create, get, and streaming payloads expose specialist states, specialist handoffs, route lane, route plan, route attempts, and capability changes. End the plan by running the new phase-specific API tests, the full test suite, compile smoke across `maf_starter`, `autogen_dashboard`, `tests`, and `main.py`, and `node --check autogen_dashboard\\static\\app.js`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `tests/test_phase3_api.py` contains `specialist_states`
    - `tests/test_phase3_api.py` contains `route_lane`
    - `tests/test_phase3_api.py` contains `capability_changes`
    - `.\.venv\Scripts\python.exe -m unittest discover -s tests -v` exits with code 0
  </acceptance_criteria>
  <done>The specialist and routing visibility contract is protected by API and operator-surface regression checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase3_api -v`
- [ ] `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`
- [ ] `.\.venv\Scripts\python.exe -m compileall maf_starter autogen_dashboard tests main.py`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] The operator surface exposes structural views for overview, agents, routing, and lower-level artifacts/traces
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can inspect specialist and routing activity through product-facing views rather than raw logs
  </success_criteria>

<output>
After completion, create `.planning/phases/03-specialist-delegation-and-routing-visibility/03-03-SUMMARY.md`
</output>
