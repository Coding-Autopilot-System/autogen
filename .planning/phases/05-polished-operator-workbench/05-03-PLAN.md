---
phase: 05-polished-operator-workbench
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/styles.css
  - autogen_dashboard/static/app.js
  - README.md
  - tests/test_phase5_ui_contract.py
  - tests/test_phase5_operator_views.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - "The final operator workbench feels polished and deliberate, with stronger hierarchy, clearer empty states, and better active-run ergonomics."
    - "Primary run actions, selected-session emphasis, sticky controls, and notices support the operator workflow instead of competing with it."
    - "The polished UI remains backed by automated contract checks plus manual operator spot checks for readability and responsiveness."
  artifacts:
    - path: "autogen_dashboard/static/styles.css"
      provides: "Final visual polish, interaction-state tokens, responsive tuning, and component refinements for the workbench"
      min_lines: 1150
      contains: ".workspace-shell"
    - path: "autogen_dashboard/static/app.js"
      provides: "Interaction-quality refinements for sticky controls, active-run emphasis, notices, and keyboard or quick-action behavior"
      min_lines: 2700
      contains: "renderRunStatus"
    - path: "README.md"
      provides: "Operator workbench usage notes and manual verification guidance"
      min_lines: 80
      contains: "Operator Workbench"
  key_links:
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/static/styles.css"
      via: "selected-session emphasis, sticky controls, notices, and responsive interaction states"
      pattern: "selected|notice|sticky|shortcut|workspace"
    - from: "README.md"
      to: "tests/test_phase5_ui_contract.py"
      via: "documented operator workflow and matching regression expectations"
      pattern: "Operator Workbench|Timeline|Artifacts"
---

<objective>
Refine visual polish, interaction quality, and operator ergonomics so the workbench feels finished instead of provisional.

Purpose: The shell and dedicated views only become a daily-use product when the final visual hierarchy, notices, active selection behavior, responsive layout, and operator guidance are coherent.
Output: Final pass on design tokens and interaction behavior, updated usage docs, and a full regression plus manual UX verification sweep.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-polished-operator-workbench/05-CONTEXT.md
@.planning/phases/05-polished-operator-workbench/05-RESEARCH.md
@.planning/phases/05-polished-operator-workbench/05-VALIDATION.md
@.planning/phases/05-polished-operator-workbench/05-UI-SPEC.md
@autogen_dashboard/static/index.html
@autogen_dashboard/static/styles.css
@autogen_dashboard/static/app.js
@README.md
@tests/test_phase5_ui_contract.py
@tests/test_phase5_operator_views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the visual hierarchy, responsive layout, and component polish for the operator workbench</name>
  <files>autogen_dashboard/static/index.html, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/static/index.html, autogen_dashboard/static/styles.css, .planning/phases/05-polished-operator-workbench/05-UI-SPEC.md, .planning/phases/05-polished-operator-workbench/05-RESEARCH.md</read_first>
  <action>Refine the workbench shell in `autogen_dashboard/static/index.html` and `autogen_dashboard/static/styles.css` so the active run area uses a stronger visual grouping and the selected run clearly dominates the page. Add a top-level layout hook named `.workspace-shell`, tune the spacing and card hierarchy to the declared UI-SPEC scale, strengthen the selected-session and active-tab state, and make the create-run panel visually secondary once a run is selected. Add explicit responsive behavior for narrow laptop widths so the rail and workspace stack cleanly without hiding the sticky control region.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/styles.css` contains `.workspace-shell`
    - `autogen_dashboard/static/styles.css` contains `.operator-tab.active`
    - `autogen_dashboard/static/styles.css` contains `@media`
    - `autogen_dashboard/static/index.html` contains `control-panel`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The workbench now has a stronger visual hierarchy and remains usable across the targeted desktop widths.</done>
</task>

<task type="auto">
  <name>Task 2: Improve operator ergonomics with better active-run cues, notices, and quick-action behavior</name>
  <files>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, tests/test_phase5_ui_contract.py</read_first>
  <action>Improve `autogen_dashboard/static/index.html`, `autogen_dashboard/static/app.js`, and `autogen_dashboard/static/styles.css` so selected runs, active pauses, and completed actions are easier to scan during daily use. Keep the existing keyboard shortcuts, but add clearer operator-facing status and notice behavior around run completion, retry, and approval waiting states. Ensure the selected session item, the sticky control area, and the run-status strip all share one visible active-run emphasis by styling `.session-item.selected` and a dedicated active state on the run-status strip. Keep the quick-action copy focused on `Run`, `Approve`, `Reject`, and `Retry` rather than generic session language, and preserve the existing `Approve + Run` button text in the shell.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/app.js` contains `showNotice`
    - `autogen_dashboard/static/app.js` contains `renderRunStatus`
    - `autogen_dashboard/static/index.html` contains `Approve + Run`
    - `autogen_dashboard/static/styles.css` contains `.run-status-strip.is-active`
    - `autogen_dashboard/static/styles.css` contains `.session-item.selected`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The operator can understand the current safe action and active run state quickly without hunting through the page.</done>
</task>

<task type="auto">
  <name>Task 3: Document the polished operator workbench and run the full regression plus manual UX sweep</name>
  <files>README.md, tests/test_phase5_ui_contract.py, tests/test_phase5_operator_views.py</files>
  <read_first>README.md, .planning/phases/05-polished-operator-workbench/05-VALIDATION.md, tests/test_phase5_ui_contract.py, tests/test_phase5_operator_views.py</read_first>
  <action>Update `README.md` so the product UI is described as `Operator Workbench`, document the active run tabs as `Overview`, `Timeline`, `Agents`, `Routing`, and `Artifacts`, and include the manual spot-check expectations for message-family clarity, route visibility, artifact inspection, and responsive layout. Finalize the Phase 5 tests so they match the finished shell and operator-view contract, then run the targeted Phase 5 suites, the full `unittest` discovery run, Python compile smoke, and `node --check autogen_dashboard\\static\\app.js`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `README.md` contains `Operator Workbench`
    - `README.md` contains `Timeline`
    - `README.md` contains `Artifacts`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase5_ui_contract tests.test_phase5_operator_views -v` exits with code 0
    - `.\.venv\Scripts\python.exe -m unittest discover -s tests -v` exits with code 0
    - `.\.venv\Scripts\python.exe -m compileall maf_core autogen_dashboard tests main.py` exits with code 0
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The polished workbench behavior is documented and protected by the full regression and manual UX verification sweep.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase5_ui_contract tests.test_phase5_operator_views -v`
- [ ] `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`
- [ ] `.\.venv\Scripts\python.exe -m compileall maf_core autogen_dashboard tests main.py`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] Manual spot checks confirm message-family clarity, timeline and artifact readability, and responsive active-run focus
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `UI-01`, `UI-02`, and `UI-03` are fully satisfied on the product UI path
  </success_criteria>

<output>
After completion, create `.planning/phases/05-polished-operator-workbench/05-03-SUMMARY.md`
</output>
