---
phase: 05-polished-operator-workbench
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/styles.css
  - autogen_dashboard/static/app.js
  - tests/test_phase5_ui_contract.py
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup: []
must_haves:
  truths:
    - "The active run workspace reads as an operator cockpit instead of a generic dashboard panel."
    - "Human, manager, specialist, event, and approval content render through visibly distinct message families."
    - "Route, model, and stage context are visible in dedicated strips or header surfaces instead of transcript prefixes."
  artifacts:
    - path: "autogen_dashboard/static/index.html"
      provides: "Restructured operator shell with active-run hierarchy, timeline tab, and focused workspace landmarks"
      min_lines: 260
      contains: "operator-tab-timeline"
    - path: "autogen_dashboard/static/styles.css"
      provides: "Message-family styling, workbench layout hierarchy, and route or status strip tokens"
      min_lines: 900
      contains: ".message-card.manager"
    - path: "tests/test_phase5_ui_contract.py"
      provides: "Static UI contract coverage for shell landmarks, tab inventory, and message-family render hooks"
      min_lines: 120
      contains: "operator-tab-timeline"
  key_links:
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/static/index.html"
      via: "operator tab registry, run-status strips, and transcript render landmarks"
      pattern: "OPERATOR_TABS|timeline|route|status"
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/static/styles.css"
      via: "message-family role mapping and route-strip presentation classes"
      pattern: "message|route|strip|approval"
---

<objective>
Design the product-grade operator shell, message surfaces, and layout hierarchy for the active run workspace.

Purpose: Phase 5 cannot feel professional until the active run has a stronger workspace structure and the transcript stops rendering all content through nearly identical bubbles.
Output: A restructured shell, distinct message families, visible route or status strips, and regression coverage for the new UI contract.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-polished-operator-workbench/05-CONTEXT.md
@.planning/phases/05-polished-operator-workbench/05-RESEARCH.md
@.planning/phases/05-polished-operator-workbench/05-VALIDATION.md
@.planning/phases/05-polished-operator-workbench/05-UI-SPEC.md
@autogen_dashboard/static/index.html
@autogen_dashboard/static/styles.css
@autogen_dashboard/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the operator shell around an active-run workspace and timeline-first tab model</name>
  <files>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js</files>
  <read_first>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, .planning/phases/05-polished-operator-workbench/05-CONTEXT.md, .planning/phases/05-polished-operator-workbench/05-UI-SPEC.md</read_first>
  <action>Update `autogen_dashboard/static/index.html` so the product chrome and workspace copy refer to `Operator Workbench` rather than `AutoGen Dashboard`, add a new tab button with `id="operator-tab-timeline"` and `data-operator-tab="timeline"`, and add dedicated active-run header containers with `id="active-route-strip"` and `id="active-stage-strip"` above the transcript region. In `autogen_dashboard/static/app.js`, change `OPERATOR_TABS` to `["overview", "timeline", "agents", "routing", "artifacts"]`, update the selected-session empty-state and control copy to refer to runs and artifacts instead of generic sessions, and wire the new route and stage strip elements so they render for the selected run.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/index.html` contains `Operator Workbench`
    - `autogen_dashboard/static/index.html` contains `id="operator-tab-timeline"`
    - `autogen_dashboard/static/index.html` contains `id="active-route-strip"`
    - `autogen_dashboard/static/index.html` contains `id="active-stage-strip"`
    - `autogen_dashboard/static/app.js` contains `const OPERATOR_TABS = ["overview", "timeline", "agents", "routing", "artifacts"]`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The shell now prioritizes the active run workspace and includes timeline as a first-class operator tab.</done>
</task>

<task type="auto">
  <name>Task 2: Replace generic transcript rendering with explicit message families and route-aware message chrome</name>
  <files>autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, .planning/phases/05-polished-operator-workbench/05-RESEARCH.md, .planning/phases/05-polished-operator-workbench/05-UI-SPEC.md</read_first>
  <action>In `autogen_dashboard/static/app.js`, replace the one-size transcript map with helper functions named `messageFamilyForRole`, `renderMessageMetaStrip`, and `renderMessageCard`, and map the roles `human`, `manager`, `specialist`, `event`, `approval`, and `system` to dedicated families. Update the transcript normalization path so it preserves `source`, `metadata`, `stage`, and route-related fields from the backend payload instead of dropping them during `normalizeSessionDetail` and message mapping. Make `messageFamilyForRole` use both `message.role` and `message.source` so approval and manager content remain distinct even when they share a generic assistant-style role. Render route, model, and stage metadata through `renderMessageMetaStrip` above manager and specialist content whenever route or stage metadata exists. In `autogen_dashboard/static/styles.css`, add concrete family selectors named `.message-card.human`, `.message-card.manager`, `.message-card.specialist`, `.message-card.event`, `.message-card.approval`, and `.message-meta-strip`, with distinct background, border, and accent behavior for each family.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/app.js` contains `function messageFamilyForRole`
    - `autogen_dashboard/static/app.js` contains `function renderMessageMetaStrip`
    - `autogen_dashboard/static/app.js` contains `function renderMessageCard`
    - `autogen_dashboard/static/app.js` contains `source:`
    - `autogen_dashboard/static/app.js` contains `metadata:`
    - `autogen_dashboard/static/styles.css` contains `.message-card.manager`
    - `autogen_dashboard/static/styles.css` contains `.message-card.specialist`
    - `autogen_dashboard/static/styles.css` contains `.message-card.approval`
    - `autogen_dashboard/static/styles.css` contains `.message-meta-strip`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The transcript now differentiates actors and shows route or stage context outside plain message body text.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 5 UI contract tests for shell landmarks and message-family hooks</name>
  <files>tests/test_phase5_ui_contract.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, tests/test_phase3_api.py</read_first>
  <action>Create `tests/test_phase5_ui_contract.py` as a static contract suite that reads the shipped `index.html`, `app.js`, and `styles.css` files and asserts the presence of the `operator-tab-timeline` button, the `active-route-strip` and `active-stage-strip` landmarks, the `OPERATOR_TABS` timeline entry, the `messageFamilyForRole` and `renderMessageMetaStrip` helpers, transcript normalization fields for `source` and `metadata`, and the CSS selectors for manager, specialist, event, and approval message families. End the task by running the new test file and `node --check autogen_dashboard\\static\\app.js`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase5_ui_contract -v</verify>
  <acceptance_criteria>
    - `tests/test_phase5_ui_contract.py` contains `operator-tab-timeline`
    - `tests/test_phase5_ui_contract.py` contains `messageFamilyForRole`
    - `tests/test_phase5_ui_contract.py` contains `source`
    - `tests/test_phase5_ui_contract.py` contains `active-route-strip`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase5_ui_contract -v` exits with code 0
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The core shell and message-family contract is now protected against regression.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase5_ui_contract -v`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] Active run shell contains timeline tab and dedicated route or stage strips
- [ ] Human, manager, specialist, event, and approval content no longer share one generic bubble treatment
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `UI-01` is materially satisfied and the workspace groundwork for `UI-02` is in place
  </success_criteria>

<output>
After completion, create `.planning/phases/05-polished-operator-workbench/05-01-SUMMARY.md`
</output>
