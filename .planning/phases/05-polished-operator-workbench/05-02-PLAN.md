---
phase: 05-polished-operator-workbench
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/app.js
  - autogen_dashboard/static/styles.css
  - tests/test_phase5_operator_views.py
  - tests/test_phase3_api.py
autonomous: true
requirements:
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - "The operator can switch between overview, timeline, agents, routing, and artifacts without reading raw logs."
    - "Timeline, routing, approval, validation, and artifact state render through dedicated operator cards instead of transcript-only content."
    - "The product UI stays backed by the existing session payload contract and explicitly protects the fields it depends on."
  artifacts:
    - path: "autogen_dashboard/static/app.js"
      provides: "Dedicated renderers and view-model helpers for timeline, agent, routing, and artifact surfaces"
      min_lines: 2500
      contains: "renderTimelineTab"
    - path: "autogen_dashboard/static/styles.css"
      provides: "Card systems for timeline rows, route attempts, agent surfaces, and artifact inspection"
      min_lines: 1050
      contains: ".timeline-card"
    - path: "tests/test_phase5_operator_views.py"
      provides: "Operator-view contract coverage for timeline, routing, agent, artifact, and event-feed rendering dependencies"
      min_lines: 140
      contains: "events"
  key_links:
    - from: "autogen_dashboard/static/app.js"
      to: "tests/test_phase5_operator_views.py"
      via: "timeline, routing, and artifact render helpers protected by static and payload tests"
      pattern: "timeline|routing|artifact|approval"
    - from: "tests/test_phase3_api.py"
      to: "autogen_dashboard/static/app.js"
      via: "API payload expectations that the product UI depends on"
      pattern: "route|specialist|artifact|approval"
---

<objective>
Build the dedicated timeline, agent, routing, and artifact inspection surfaces into the product UI.

Purpose: The workbench only becomes operator-grade when the user can inspect what happened, who did it, how the route behaved, and what changed without scanning raw transcript or event text.
Output: Dedicated `Timeline`, `Agents`, `Routing`, and `Artifacts` product views plus regression coverage for the fields and renderers they depend on.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-polished-operator-workbench/05-CONTEXT.md
@.planning/phases/05-polished-operator-workbench/05-RESEARCH.md
@.planning/phases/05-polished-operator-workbench/05-VALIDATION.md
@.planning/phases/05-polished-operator-workbench/05-UI-SPEC.md
@autogen_dashboard/static/index.html
@autogen_dashboard/static/app.js
@autogen_dashboard/static/styles.css
@autogen_dashboard/session_runner.py
@autogen_dashboard/schemas.py
@tests/test_phase3_api.py
@tests/test_phase4_approval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build timeline and operator inspection view models from the existing session contract</name>
  <files>autogen_dashboard/static/app.js</files>
  <read_first>autogen_dashboard/static/app.js, autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py, .planning/phases/05-polished-operator-workbench/05-RESEARCH.md</read_first>
  <action>Extend `autogen_dashboard/static/app.js` with explicit helpers named `buildTimelineEntries`, `buildOperatorArtifactSections`, and `buildRoutingSummary`. First, update `normalizeSessionDetail` so it preserves the session `events` array from the backend payload alongside `stageTimeline`, route data, approvals, and validation results. `buildTimelineEntries` must then combine `stageTimeline`, `events`, `pendingApproval`, `validationResults`, and route-attempt data into one operator-facing sequence. `buildOperatorArtifactSections` must group `stageOutputs`, `diffArtifacts`, `validationResults`, and `artifacts` into stage-labelled inspection sections. `buildRoutingSummary` must expose requested lane, planned route, actual provider and model, fallback count, and capability drift for use across Overview and Routing.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/app.js` contains `function buildTimelineEntries`
    - `autogen_dashboard/static/app.js` contains `function buildOperatorArtifactSections`
    - `autogen_dashboard/static/app.js` contains `function buildRoutingSummary`
    - `autogen_dashboard/static/app.js` contains `events:`
    - `autogen_dashboard/static/app.js` contains `pendingApproval`
    - `autogen_dashboard/static/app.js` contains `validationResults`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The UI now has one coherent view-model layer for timeline, routing, and artifacts instead of scattered per-tab logic.</done>
</task>

<task type="auto">
  <name>Task 2: Render Timeline, Agents, Routing, and Artifacts as dedicated operator views with product-grade cards</name>
  <files>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, .planning/phases/05-polished-operator-workbench/05-UI-SPEC.md</read_first>
  <action>Update the tab renderers in `autogen_dashboard/static/app.js` so `renderTimelineTab`, `renderAgentsTab`, `renderRoutingTab`, and `renderArtifactsTab` each output dedicated operator cards rather than generic text lists. Use concrete CSS hooks named `.timeline-list`, `.timeline-card`, `.route-summary-card`, `.artifact-detail-card`, and `.agent-activity-card` in `autogen_dashboard/static/styles.css`. Keep the tab labels exactly `Overview`, `Timeline`, `Agents`, `Routing`, and `Artifacts`, and ensure the Timeline view surfaces approvals, validation steps, and fallback events while the Artifacts view surfaces changed files, diff artifacts, and validation artifacts first.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/static/app.js` contains `function renderTimelineTab`
    - `autogen_dashboard/static/app.js` contains `function renderArtifactsTab`
    - `autogen_dashboard/static/styles.css` contains `.timeline-card`
    - `autogen_dashboard/static/styles.css` contains `.route-summary-card`
    - `autogen_dashboard/static/styles.css` contains `.artifact-detail-card`
    - `autogen_dashboard/static/styles.css` contains `.agent-activity-card`
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The operator can inspect each major run dimension through dedicated product views instead of transcript-only or raw-log surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Add operator-view regression coverage for timeline, routing, and artifact dependencies</name>
  <files>tests/test_phase5_operator_views.py, tests/test_phase3_api.py, autogen_dashboard/static/app.js</files>
  <read_first>tests/test_phase3_api.py, autogen_dashboard/static/app.js, autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py, tests/test_phase4_approval.py</read_first>
  <action>Create `tests/test_phase5_operator_views.py` to assert that the UI contract has the render helpers and payload dependencies required for timeline, routing, specialist, approval, validation, artifact, and structured event surfaces. Extend `tests/test_phase3_api.py` so the fake session payload and assertions include the timeline, `events`, route-attempt, diff-artifact, validation-result, and pending-approval fields the new product views rely on. End the task by running the new Phase 5 operator-view test together with `tests.test_phase3_api`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase5_operator_views tests.test_phase3_api -v</verify>
  <acceptance_criteria>
    - `tests/test_phase5_operator_views.py` contains `buildTimelineEntries`
    - `tests/test_phase5_operator_views.py` contains `buildOperatorArtifactSections`
    - `tests/test_phase5_operator_views.py` contains `events`
    - `tests/test_phase3_api.py` contains `route_attempts`
    - `tests/test_phase3_api.py` contains `events`
    - `tests/test_phase3_api.py` contains `validation_results`
    - `tests/test_phase3_api.py` contains `diff_artifacts`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase5_operator_views tests.test_phase3_api -v` exits with code 0
  </acceptance_criteria>
  <done>The dedicated operator views are now protected by static and API-facing regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase5_operator_views tests.test_phase3_api -v`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] Timeline, routing, agents, and artifacts all render as dedicated views without raw-log dependence
- [ ] Timeline view is backed by structured `events` plus `stageTimeline`, not transcript scraping alone
- [ ] Approvals, validation results, and changed files are visible through cards or grouped sections instead of transcript text
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `UI-02` and `UI-03` are materially satisfied on the product UI path
  </success_criteria>

<output>
After completion, create `.planning/phases/05-polished-operator-workbench/05-02-SUMMARY.md`
</output>
