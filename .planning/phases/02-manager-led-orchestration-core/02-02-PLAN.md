---
phase: 02-manager-led-orchestration-core
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - maf_starter/gsd_autofill.py
  - maf_starter/tools.py
  - maf_starter/orchestration.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_store.py
  - autogen_dashboard/session_runner.py
  - tests/test_phase2_runtime.py
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03
  - ORCH-04
user_setup: []
must_haves:
  truths:
    - "A paused run can continue from the paused stage after approval, missing input, or retry without losing prior stage outputs."
    - "Routine GSD clarification questions are answered automatically from project and repo context when confidence is high enough."
    - "Automatic GSD answers and unresolved question packs are persisted as structured artifacts and events."
  artifacts:
    - path: "maf_starter/gsd_autofill.py"
      provides: "Context-first resolution of routine GSD clarification questions"
      min_lines: 100
      contains: "PROJECT.md"
    - path: "autogen_dashboard/session_runner.py"
      provides: "Stage-aware pause, resume, retry, and auto-answer lifecycle"
      min_lines: 350
      contains: "needs_input"
    - path: "tests/test_phase2_runtime.py"
      provides: "Regression coverage for stage-scoped resume, retry, and GSD auto-answer handling"
      min_lines: 120
      contains: "auto_answer"
  key_links:
    - from: "autogen_dashboard/session_runner.py"
      to: "maf_starter/gsd_autofill.py"
      via: "automatic clarification resolution"
      pattern: "auto_answer|gsd"
    - from: "autogen_dashboard/session_runner.py"
      to: "autogen_dashboard/schemas.py"
      via: "stage and pause contract"
      pattern: "current_stage|pause_reason|stage_timeline"
---

<objective>
Add structured pause/resume behavior and automatic GSD-question handling on top of the manager workflow.

Purpose: The manager contract is incomplete until it can pause cleanly, resume deterministically, and answer routine clarification questions without repeatedly interrupting the operator.
Output: Stage-aware pause/resume state, a GSD auto-answer resolver, and regression tests for operator and runtime behavior.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md
@.planning/phases/02-manager-led-orchestration-core/02-RESEARCH.md
@maf_starter/orchestration.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_store.py
@autogen_dashboard/session_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a structured GSD auto-answer resolver</name>
  <files>maf_starter/gsd_autofill.py, maf_starter/tools.py, maf_starter/orchestration.py</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md, maf_starter/tools.py, maf_starter/orchestration.py</read_first>
  <action>Create `maf_starter/gsd_autofill.py` with a context resolver that reads `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, current phase context, current workspace snapshot, and repo-inspection output to answer routine GSD clarification questions. The resolver must return a structured result with `question`, `answer`, `sources`, `confidence`, `decision_type`, and `needs_input` fields. Update `maf_starter/tools.py` or a neighboring runtime seam so the manager can request context snapshots for auto-answering without bypassing repo-root safety. Extend `maf_starter/orchestration.py` so `AutoAnswerRecord` can serialize both successful answers and unresolved question packs.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_runtime -v</verify>
  <acceptance_criteria>
    - `maf_starter/gsd_autofill.py` contains `PROJECT.md`
    - `maf_starter/gsd_autofill.py` contains `confidence`
    - `maf_starter/orchestration.py` contains `AutoAnswerRecord`
    - `tests/test_phase2_runtime.py` exits with code 0
  </acceptance_criteria>
  <done>The runtime can answer routine GSD questions from structured context instead of always pausing the operator.</done>
</task>

<task type="auto">
  <name>Task 2: Make the durable run model stage-aware for pause, resume, and retry</name>
  <files>autogen_dashboard/schemas.py, autogen_dashboard/session_store.py, autogen_dashboard/session_runner.py</files>
  <read_first>autogen_dashboard/schemas.py, autogen_dashboard/session_store.py, autogen_dashboard/session_runner.py, maf_starter/orchestration.py, .planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md</read_first>
  <action>Extend the persisted run schemas and store so runs carry `current_stage`, `last_completed_stage`, `stage_timeline`, `stage_outputs`, `auto_answer_records`, `blocked_questions`, and `pause_kind` fields in addition to the existing Phase 1 run metadata. Update `autogen_dashboard/session_runner.py` so stage-aware runs can pause with `needs_input`, `needs_approval`, `blocked`, `retryable_error`, `completed`, or `stopped`, resume the current stage with prior outputs intact, and perform stage-scoped retry without replaying successful prior stages. Persist structured artifacts under `artifacts/stages/` and `artifacts/gsd/` plus matching `stage.*` and `gsd.*` events in the run event stream.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_runtime -v</verify>
  <acceptance_criteria>
    - `autogen_dashboard/schemas.py` contains `current_stage`
    - `autogen_dashboard/schemas.py` contains `stage_timeline`
    - `autogen_dashboard/session_runner.py` contains `stage.started`
    - `autogen_dashboard/session_runner.py` contains `gsd.answer.generated`
    - `autogen_dashboard/session_runner.py` contains `retryable_error`
    - `tests/test_phase2_runtime.py` exits with code 0
  </acceptance_criteria>
  <done>The durable run contract now carries stage-aware orchestration state, stage artifacts, and auto-answer provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime regression coverage for pause/resume and auto-answer behavior</name>
  <files>tests/test_phase2_runtime.py, tests/test_run_persistence.py</files>
  <read_first>tests/test_phase1_runtime.py, tests/test_run_persistence.py, autogen_dashboard/session_runner.py, autogen_dashboard/session_store.py</read_first>
  <action>Create `tests/test_phase2_runtime.py` to verify stage-scoped pause/resume transitions, stage-scoped retry behavior, persisted stage outputs, auto-answer generation from project or phase context, and unresolved-question fallback to `needs_input`. Extend `tests/test_run_persistence.py` with assertions that stage artifacts and GSD answer artifacts are written into the run directory and survive reopen or retry. The tests must prove that successful early stages are preserved when a later stage pauses or retries.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_runtime tests.test_run_persistence -v</verify>
  <acceptance_criteria>
    - `tests/test_phase2_runtime.py` contains `needs_input`
    - `tests/test_phase2_runtime.py` contains `gsd.answer.generated`
    - `tests/test_run_persistence.py` contains `artifacts/stages`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase2_runtime tests.test_run_persistence -v` exits with code 0
  </acceptance_criteria>
  <done>Stage-aware pause, resume, and automatic GSD-answer behavior is protected by regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase2_runtime tests.test_run_persistence -v`
- [ ] Paused runs resume from the current stage instead of replaying the full workflow
- [ ] Automatic GSD answers and unresolved question packs are persisted as structured artifacts and events
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ORCH-02`, `ORCH-03`, and `ORCH-04` are satisfied at the durable runtime contract level
  </success_criteria>

<output>
After completion, create `.planning/phases/02-manager-led-orchestration-core/02-02-SUMMARY.md`
</output>
