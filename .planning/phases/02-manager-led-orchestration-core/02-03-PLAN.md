---
phase: 02-manager-led-orchestration-core
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - maf_core/provider_fallback.py
  - maf_core/devui_patches.py
  - autogen_dashboard/app.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/session_runner.py
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/app.js
  - autogen_dashboard/static/styles.css
  - tests/test_phase2_api.py
  - tests/test_maf_setup.py
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
user_setup: []
must_haves:
  truths:
    - "The operator can see current stage, overall run status, pause reason, last completed stage, and stage outputs without reading raw event logs."
    - "Structured stage and run events are emitted consistently across runtime, persistence, and API payloads."
    - "Route and fallback metadata remain visible alongside orchestration summaries so capability changes are not hidden."
  artifacts:
    - path: "autogen_dashboard/session_runner.py"
      provides: "Structured `run.*`, `stage.*`, and `gsd.*` event emission aligned with the active runtime"
      min_lines: 380
      contains: "stage.started"
    - path: "autogen_dashboard/static/app.js"
      provides: "Operator-facing orchestration summary and stage timeline rendering"
      min_lines: 1400
      contains: "stage"
    - path: "tests/test_phase2_api.py"
      provides: "API contract coverage for stage visibility, pause semantics, and orchestration summaries"
      min_lines: 120
      contains: "current_stage"
  key_links:
    - from: "autogen_dashboard/app.py"
      to: "autogen_dashboard/session_runner.py"
      via: "orchestration summary payloads"
      pattern: "stage|pause"
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/schemas.py"
      via: "current-stage, last-stage, and timeline rendering"
      pattern: "currentStage|stageTimeline|pause"
    - from: "maf_core/provider_fallback.py"
      to: "autogen_dashboard/session_runner.py"
      via: "capability-aware route metadata in orchestration summaries"
      pattern: "active_provider|active_model|tools_available"
---

<objective>
Expose the new orchestration contract cleanly through the active runtime, persisted run payloads, and operator-facing surface.

Purpose: Phase 2 only becomes usable when the operator can understand current stage, pause reason, and stage outputs without reading raw logs or DevUI internals.
Output: Structured orchestration events, aligned API payloads, and a temporary operator surface that clearly exposes run-stage state.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md
@.planning/phases/02-manager-led-orchestration-core/02-RESEARCH.md
@autogen_dashboard/app.py
@autogen_dashboard/schemas.py
@autogen_dashboard/session_runner.py
@autogen_dashboard/static/index.html
@autogen_dashboard/static/app.js
@autogen_dashboard/static/styles.css
@maf_core/provider_fallback.py
@maf_core/devui_patches.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit aligned run and stage events through the active runtime and durable run model</name>
  <files>autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py, maf_core/provider_fallback.py, maf_core/devui_patches.py</files>
  <read_first>autogen_dashboard/session_runner.py, autogen_dashboard/schemas.py, maf_core/provider_fallback.py, maf_core/devui_patches.py, maf_core/orchestration.py</read_first>
  <action>Extend the runtime and persistence layer so `run.started`, `run.completed`, `run.failed`, `stage.started`, `stage.completed`, `stage.paused`, `stage.blocked`, and `gsd.answer.generated` are emitted as structured events with stage name, stage status, artifact references, pause kind, and capability metadata. Update `maf_core/provider_fallback.py` so route metadata exposes `active_provider`, `active_model`, `route_tier`, `route_reason`, `fallback_used`, and `tools_available` in a way the orchestration layer can attach to stage summaries. Use `maf_core/devui_patches.py` only for local trace enrichment, not as the source of truth for stage state.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_api tests.test_maf_setup -v</verify>
  <acceptance_criteria>
    - `autogen_dashboard/session_runner.py` contains `stage.started`
    - `autogen_dashboard/session_runner.py` contains `stage.completed`
    - `autogen_dashboard/session_runner.py` contains `run.failed`
    - `maf_core/provider_fallback.py` contains `tools_available`
    - `tests/test_phase2_api.py` exits with code 0
  </acceptance_criteria>
  <done>Structured orchestration and route metadata are emitted consistently across runtime and persistence layers.</done>
</task>

<task type="auto">
  <name>Task 2: Expose orchestration summaries in the operator API and current UI shell</name>
  <files>autogen_dashboard/app.py, autogen_dashboard/schemas.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/app.py, autogen_dashboard/schemas.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, .planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md</read_first>
  <action>Update the operator API contract so run detail and summary payloads include `current_stage`, `last_completed_stage`, `stage_timeline`, `stage_outputs`, `pause_kind`, and the latest route metadata. Update the current operator shell to render a compact orchestration panel with rounded cards for current stage, overall run status, last completed stage, pause reason, and stage-output links or summaries. Add a stage timeline or stage list view that lets the operator see which stages are pending, running, paused, blocked, completed, or failed without opening raw event logs. Keep the design aligned with the existing rounded-card direction, but do not attempt the final polished product shell yet.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/app.py` contains `current_stage`
    - `autogen_dashboard/schemas.py` contains `last_completed_stage`
    - `autogen_dashboard/static/index.html` contains a stage or orchestration summary container
    - `autogen_dashboard/static/app.js` contains `stageTimeline` or `currentStage`
    - `autogen_dashboard/static/styles.css` contains a dedicated orchestration panel style block
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The current operator-facing shell exposes orchestration state explicitly instead of relying on raw traces.</done>
</task>

<task type="auto">
  <name>Task 3: Add API and smoke coverage for orchestration visibility</name>
  <files>tests/test_phase2_api.py, tests/test_maf_setup.py</files>
  <read_first>tests/test_phase1_api.py, tests/test_maf_setup.py, autogen_dashboard/app.py, autogen_dashboard/schemas.py, maf_core/provider_fallback.py</read_first>
  <action>Create `tests/test_phase2_api.py` to verify that create/get/run/retry payloads expose current stage, last completed stage, stage timeline, pause kind, and route metadata. Extend `tests/test_maf_setup.py` with smoke assertions for local orchestration trace metadata so the active runtime still builds and emits route fields after the Phase 2 changes. End the plan by running the Phase 2 API tests, the full suite, a compile smoke across `maf_core`, `autogen_dashboard`, `tests`, and `main.py`, and `node --check autogen_dashboard\\static\\app.js`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `tests/test_phase2_api.py` contains `current_stage`
    - `tests/test_phase2_api.py` contains `pause_kind`
    - `tests/test_maf_setup.py` contains `tools_available`
    - `.\.venv\Scripts\python.exe -m unittest discover -s tests -v` exits with code 0
  </acceptance_criteria>
  <done>The orchestration visibility contract is locked by API tests and runtime smoke checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase2_api tests.test_maf_setup -v`
- [ ] `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`
- [ ] `.\.venv\Scripts\python.exe -m compileall maf_core autogen_dashboard tests main.py`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] The operator surface shows stage progress and pause reasons without reading raw event logs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The orchestration contract is visible, inspectable, and consistent across runtime, persistence, and operator-facing state
  </success_criteria>

<output>
After completion, create `.planning/phases/02-manager-led-orchestration-core/02-03-SUMMARY.md`
</output>
