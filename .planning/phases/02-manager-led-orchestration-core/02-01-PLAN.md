---
phase: 02-manager-led-orchestration-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - maf_core/orchestration.py
  - maf_core/team_factory.py
  - maf_core/workflow_factory.py
  - maf_core/agent_factory.py
  - entities/repo_team/workflow.py
  - tests/test_phase2_manager.py
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
user_setup: []
must_haves:
  truths:
    - "One prompt can drive a manager-owned workflow through the canonical `planning -> research -> implementation -> review -> validation` stage order."
    - "The manager workflow persists explicit stage records and stage summaries under one durable run identity."
    - "Stage transitions are driven by structured orchestration state rather than transcript parsing or DevUI-only assumptions."
  artifacts:
    - path: "maf_core/orchestration.py"
      provides: "Shared orchestration types, canonical stage names, pause/status vocabulary, and serializable stage-state helpers"
      min_lines: 120
      contains: "class RunOrchestrationState"
    - path: "maf_core/team_factory.py"
      provides: "Manager-led workflow builder that owns the canonical stage sequence"
      min_lines: 80
      contains: "planning"
    - path: "tests/test_phase2_manager.py"
      provides: "Regression coverage for stage order, stage persistence, and manager-owned transitions"
      min_lines: 80
      contains: "planning"
  key_links:
    - from: "maf_core/team_factory.py"
      to: "maf_core/orchestration.py"
      via: "shared stage-state contract"
      pattern: "RunOrchestrationState|StageRecord|StageStatus"
    - from: "maf_core/workflow_factory.py"
      to: "maf_core/orchestration.py"
      via: "orchestration artifact persistence"
      pattern: "orchestration|stage"
---

<objective>
Introduce the shared manager orchestration contract and make the current specialist workflow execute against it.

Purpose: Phase 2 cannot succeed until the runtime has one canonical stage machine that both execution and operator state can trust.
Output: A manager-owned orchestration module, a workflow builder that uses it, and regression tests that lock the stage order and persistence shape.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md
@.planning/phases/02-manager-led-orchestration-core/02-RESEARCH.md
@maf_core/team_factory.py
@maf_core/workflow_factory.py
@maf_core/agent_factory.py
@entities/repo_team/workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared orchestration state contract</name>
  <files>maf_core/orchestration.py, maf_core/workflow_factory.py</files>
  <read_first>maf_core/workflow_factory.py, .planning/phases/02-manager-led-orchestration-core/02-CONTEXT.md, .planning/phases/02-manager-led-orchestration-core/02-RESEARCH.md</read_first>
  <action>Create `maf_core/orchestration.py` with explicit phase-2 runtime types and helpers: `StageName` values `planning`, `research`, `implementation`, `review`, `validation`; `StageStatus` values `pending`, `running`, `paused`, `blocked`, `completed`, and `failed`; `RunStagePauseKind` values `needs_input`, `needs_approval`, `blocked`, `retryable_error`, `completed`, and `stopped`; serializable dataclasses or equivalent for `StageRecord`, `StageSummary`, `RunOrchestrationState`, and `AutoAnswerRecord`; helper methods to start, complete, pause, fail, and retry a stage; and `to_dict`/`from_dict` helpers for persistence. Extend `maf_core/workflow_factory.py` so a run-scoped workflow can persist orchestration snapshots and stage artifacts under `runtime/orchestration/` and `artifacts/stages/` instead of relying only on raw checkpoints.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_manager</verify>
  <acceptance_criteria>
    - `maf_core/orchestration.py` contains `class RunOrchestrationState`
    - `maf_core/orchestration.py` contains `planning`
    - `maf_core/orchestration.py` contains `retryable_error`
    - `maf_core/workflow_factory.py` contains `orchestration`
    - `tests/test_phase2_manager.py` exits with code 0
  </acceptance_criteria>
  <done>The shared stage machine exists as a first-class runtime contract with run-scoped persistence helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the current specialist workflow into a manager-owned stage flow</name>
  <files>maf_core/team_factory.py, maf_core/agent_factory.py, entities/repo_team/workflow.py</files>
  <read_first>maf_core/team_factory.py, maf_core/agent_factory.py, entities/repo_team/workflow.py, maf_core/orchestration.py</read_first>
  <action>Refactor `maf_core/team_factory.py` so the existing specialist participants execute under an explicit manager-owned stage contract instead of only a raw sequential workflow. The manager must map `planner -> planning`, `researcher -> research`, `implementer -> implementation`, and `reviewer -> review`, and it must create a terminal `validation` stage summary even when validation is still a lightweight placeholder in Phase 2. Update role instructions in `maf_core/agent_factory.py` or stage-specific builders so specialist outputs are emitted as structured stage summaries with `summary`, `artifacts`, `next_action`, and `needs_approval` or `needs_input` hints. Keep `entities/repo_team/workflow.py` as the active entity surface, but make its description clearly state that it is the manager-led orchestration workflow for engineering runs.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_manager</verify>
  <acceptance_criteria>
    - `maf_core/team_factory.py` contains `validation`
    - `maf_core/team_factory.py` contains `RunOrchestrationState`
    - `maf_core/agent_factory.py` contains `needs_approval`
    - `entities/repo_team/workflow.py` contains `workflow =`
    - `tests/test_phase2_manager.py` exits with code 0
  </acceptance_criteria>
  <done>The active multi-agent workflow is manager-led and emits explicit stage-aware outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for canonical stage order and stage persistence</name>
  <files>tests/test_phase2_manager.py, tests/test_maf_setup.py</files>
  <read_first>tests/test_maf_setup.py, maf_core/team_factory.py, maf_core/workflow_factory.py, maf_core/orchestration.py</read_first>
  <action>Create `tests/test_phase2_manager.py` to verify the canonical stage order, stage status transitions, serialization round-trips for orchestration state, and run-scoped stage artifact paths. Extend `tests/test_maf_setup.py` with smoke checks that the active `repo_team` workflow still builds without network access and exposes the manager-led description. The test must assert that stage records persist under one run-scoped location and that stage retry targets a named stage rather than forcing a full new run.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase2_manager tests.test_maf_setup -v</verify>
  <acceptance_criteria>
    - `tests/test_phase2_manager.py` contains `RunOrchestrationState`
    - `tests/test_phase2_manager.py` contains `planning`
    - `tests/test_phase2_manager.py` contains `validation`
    - `tests/test_maf_setup.py` contains `repo_team`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase2_manager tests.test_maf_setup -v` exits with code 0
  </acceptance_criteria>
  <done>The canonical manager stage contract is protected by repeatable automated checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase2_manager tests.test_maf_setup -v`
- [ ] The manager workflow persists stage state and stage artifacts under one durable run identity
- [ ] The canonical stage order is explicit in runtime code and not inferred from raw transcript text
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `ORCH-01` and the stage-visibility core of `ORCH-02` are established in the active runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/02-manager-led-orchestration-core/02-01-SUMMARY.md`
</output>
