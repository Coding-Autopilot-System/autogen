---
phase: 06-api-boundary-and-control-plane-contract
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - maf_core/control_plane/router.py
  - maf_core/control_plane/auth.py
  - command_center/app.py
  - tests/test_phase6_api_contract.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
user_setup: []
must_haves:
  truths:
    - "A versioned `/api/v1/runs` router exists and exposes resource-oriented run endpoints."
    - "The router satisfies API-01 (create run), API-02 (get run status), API-03 (inspect timeline/agents/routing/artifacts), and API-04 (control actions)."
    - "Command Center HTTP app mounts the shared `/api/v1` router so the active product shell and external callers use the same contract."
  artifacts:
    - path: "maf_core/control_plane/router.py"
      provides: "/api/v1 FastAPI router with run CRUD and control actions"
      min_lines: 200
      contains: "/api/v1/runs"
    - path: "maf_core/control_plane/auth.py"
      provides: "Pluggable auth policy boundary, loopback-open by default"
      min_lines: 40
      contains: "AuthPolicy"
    - path: "command_center/app.py"
      provides: "Command Center mounts shared /api/v1 router"
      min_lines: 150
      contains: "app.include_router"
    - path: "tests/test_phase6_api_contract.py"
      provides: "REST contract validation for API-01 through API-04"
      min_lines: 200
      contains: "test_create_run_api"
  key_links:
    - from: "maf_core/control_plane/router.py"
      to: "maf_core/control_plane/service.py"
      via: "FastAPI dependency injection to RunService"
      pattern: "RunService|Depends"
    - from: "maf_core/control_plane/router.py"
      to: "maf_core/control_plane/auth.py"
      via: "Pluggable auth boundary on all endpoints"
      pattern: "AuthPolicy|require_auth"
    - from: "command_center/app.py"
      to: "maf_core/control_plane/router.py"
      via: "Router mounting for shared API contract"
      pattern: "include_router|/api/v1"
    - from: "tests/test_phase6_api_contract.py"
      to: "maf_core/control_plane/router.py"
      via: "TestClient validation of API-01, API-02, API-03, API-04"
      pattern: "POST /api/v1/runs|GET /api/v1/runs"
---

<objective>
Deliver a versioned `/api/v1/runs` REST router that exposes the shared control-plane contract and mount it in Command Center.

Purpose: Phase 6 cannot satisfy API requirements until the shared service has a stable, resource-oriented HTTP surface.
Output: A complete `/api/v1` router, auth boundary, Command Center integration, and REST contract validation.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-api-boundary-and-control-plane-contract/06-CONTEXT.md
@.planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md
@maf_core/control_plane/service.py
@maf_core/control_plane/contracts.py
@command_center/app.py
@autogen_dashboard/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pluggable auth policy boundary</name>
  <files>maf_core/control_plane/auth.py</files>
  <read_first>.planning/phases/06-api-boundary-and-control-plane-contract/06-CONTEXT.md, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `maf_core/control_plane/auth.py` with an `AuthPolicy` protocol or abstract base class defining `require_auth(request: Request) -> dict | None` that returns caller identity or raises HTTPException(401/403). Implement a default `NoAuthPolicy` that returns `{"caller": "local"}` for loopback development. Add a `get_auth_policy() -> AuthPolicy` factory that returns the active policy from env or default. This allows Phase 7 to plug in Azure Functions auth without touching endpoint handlers.</action>
  <verify>python -m compileall maf_core/control_plane</verify>
  <acceptance_criteria>
    - `maf_core/control_plane/auth.py` contains `class AuthPolicy`
    - `maf_core/control_plane/auth.py` contains `def require_auth`
    - `maf_core/control_plane/auth.py` contains `NoAuthPolicy`
    - `maf_core/control_plane/auth.py` contains `get_auth_policy`
    - `python -m compileall maf_core/control_plane` exits with code 0
  </acceptance_criteria>
  <done>Auth is now a pluggable boundary ready for cloud integration in Phase 7.</done>
</task>

<task type="auto">
  <name>Task 2: Build /api/v1 FastAPI router with resource-oriented run endpoints</name>
  <files>maf_core/control_plane/router.py</files>
  <read_first>maf_core/control_plane/service.py, maf_core/control_plane/contracts.py, maf_core/control_plane/auth.py, autogen_dashboard/app.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `maf_core/control_plane/router.py` with a FastAPI `APIRouter(prefix="/api/v1")`. Add endpoints: `POST /runs` (API-01: create run, accept CreateRunRequest, return RunSummary with run_id). `GET /runs` (list runs, return list of RunSummary). `GET /runs/{run_id}` (API-02: get run status, return RunDetail with current_stage, pause_kind, routing, workspace). `GET /runs/{run_id}/timeline` (API-03: stage timeline). `GET /runs/{run_id}/agents` (API-03: specialist states). `GET /runs/{run_id}/routing` (API-03: route history and capability drift). `GET /runs/{run_id}/artifacts` (API-03: artifact manifest). `GET /runs/{run_id}/artifacts/{artifact_path:path}` (API-03: fetch specific artifact). `GET /runs/{run_id}/events` (API-03: event log). `POST /runs/{run_id}/actions/continue` (API-04: resume paused run). `POST /runs/{run_id}/actions/approve` (API-04: approve pending approval). `POST /runs/{run_id}/actions/retry` (API-04: retry failed run). `POST /runs/{run_id}/actions/cancel` (API-04: cancel run). `POST /runs/{run_id}/actions/operator-input` (API-04: append operator message). Use FastAPI Depends for RunService injection and auth policy on all endpoints. Return appropriate HTTP status codes (201 for create, 404 for missing run, 400 for invalid transitions).</action>
  <verify>python -m compileall maf_core/control_plane</verify>
  <acceptance_criteria>
    - `maf_core/control_plane/router.py` contains `APIRouter(prefix="/api/v1")`
    - `maf_core/control_plane/router.py` contains `POST /runs`
    - `maf_core/control_plane/router.py` contains `GET /runs/{run_id}`
    - `maf_core/control_plane/router.py` contains `GET /runs/{run_id}/timeline`
    - `maf_core/control_plane/router.py` contains `GET /runs/{run_id}/artifacts`
    - `maf_core/control_plane/router.py` contains `POST /runs/{run_id}/actions/continue`
    - `maf_core/control_plane/router.py` contains `POST /runs/{run_id}/actions/approve`
    - `maf_core/control_plane/router.py` contains `Depends`
    - `maf_core/control_plane/router.py` contains `RunService`
    - `python -m compileall maf_core/control_plane` exits with code 0
  </acceptance_criteria>
  <done>/api/v1 router is now complete and satisfies API-01 through API-04.</done>
</task>

<task type="auto">
  <name>Task 3: Mount /api/v1 router in Command Center HTTP app</name>
  <files>command_center/app.py</files>
  <read_first>command_center/app.py, maf_core/control_plane/router.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>In `command_center/app.py`, import the `/api/v1` router from `maf_core.control_plane.router`. Use `app.include_router(...)` to mount it in the `create_command_center_app(...)` factory. Preserve existing AG-UI endpoints and repo catalog routes. The Command Center now exposes both the shared control-plane API and the specialized UI transport.</action>
  <verify>python -m compileall command_center</verify>
  <acceptance_criteria>
    - `command_center/app.py` contains `from maf_core.control_plane.router import`
    - `command_center/app.py` contains `app.include_router`
    - `command_center/app.py` contains `/api/v1`
    - `python -m compileall command_center` exits with code 0
  </acceptance_criteria>
  <done>Command Center now serves the shared /api/v1 contract.</done>
</task>

<task type="auto">
  <name>Task 4: Add REST contract validation tests for API-01 through API-04</name>
  <files>tests/test_phase6_api_contract.py</files>
  <read_first>maf_core/control_plane/router.py, command_center/app.py, tests/test_command_center.py, tests/test_phase3_api.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `tests/test_phase6_api_contract.py` using `fastapi.testclient.TestClient` with `create_command_center_app(...)` following patterns in `tests/test_command_center.py` and `tests/test_phase3_api.py`. Add tests: `test_create_run_api` validates POST /api/v1/runs returns 201 with run_id and RunSummary (API-01). `test_get_run_api` validates GET /api/v1/runs/{run_id} returns status, current_stage, pause_kind, routing (API-02). `test_timeline_agents_routing_artifacts_api` validates GET /api/v1/runs/{run_id}/timeline, /agents, /routing, /artifacts return structured details (API-03). `test_control_actions_api` validates POST /api/v1/runs/{run_id}/actions/continue, /approve, /retry, /cancel update the run (API-04). `test_artifact_fetch_api` validates GET /api/v1/runs/{run_id}/artifacts/{path} returns manifest-relative artifacts only. `test_missing_run_404` validates proper error handling. Run the test suite at task end.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase6_api_contract -v</verify>
  <acceptance_criteria>
    - `tests/test_phase6_api_contract.py` contains `test_create_run_api`
    - `tests/test_phase6_api_contract.py` contains `test_get_run_api`
    - `tests/test_phase6_api_contract.py` contains `test_timeline_agents_routing_artifacts_api`
    - `tests/test_phase6_api_contract.py` contains `test_control_actions_api`
    - `tests/test_phase6_api_contract.py` contains `test_artifact_fetch_api`
    - `tests/test_phase6_api_contract.py` contains `API-01`
    - `tests/test_phase6_api_contract.py` contains `API-02`
    - `tests/test_phase6_api_contract.py` contains `POST /api/v1/runs`
    - `tests/test_phase6_api_contract.py` contains `GET /api/v1/runs`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase6_api_contract -v` exits with code 0
  </acceptance_criteria>
  <done>REST API contract for API-01 through API-04 is now validated and regression-protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase6_api_contract -v`
- [ ] `python -m compileall maf_core/control_plane command_center`
- [ ] `/api/v1/runs` router exists and is mounted in Command Center
- [ ] All API-01, API-02, API-03, and API-04 endpoints are implemented and tested
- [ ] Auth is a pluggable boundary ready for Phase 7
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- API-01 through API-04 are fully satisfied with regression coverage
- Command Center now exposes the shared control-plane contract
  </success_criteria>

<output>
After completion, create `.planning/phases/06-api-boundary-and-control-plane-contract/06-02-SUMMARY.md`
</output>
