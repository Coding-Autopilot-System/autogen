---
phase: 06-api-boundary-and-control-plane-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - maf_core/control_plane/contracts.py
  - maf_core/control_plane/store.py
  - maf_core/control_plane/service.py
  - maf_core/control_plane/__init__.py
  - tests/test_phase6_service.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
user_setup: []
must_haves:
  truths:
    - "A shared control-plane package exists under `maf_core/control_plane/` and is not UI-owned."
    - "Run contracts, persistence, and service logic are extracted from `autogen_dashboard/` donors into the shared package."
    - "The extracted service preserves durable run identity, workspace context, stage state, routing metadata, specialist state, and artifact manifests."
  artifacts:
    - path: "maf_core/control_plane/contracts.py"
      provides: "Canonical Run* request/response models migrated from autogen_dashboard/schemas.py"
      min_lines: 150
      contains: "RunSummary"
    - path: "maf_core/control_plane/store.py"
      provides: "Durable run persistence migrated from autogen_dashboard/session_store.py"
      min_lines: 200
      contains: "RunStore"
    - path: "maf_core/control_plane/service.py"
      provides: "Run lifecycle and control actions migrated from autogen_dashboard/session_runner.py"
      min_lines: 250
      contains: "RunService"
    - path: "tests/test_phase6_service.py"
      provides: "Service-level validation for create, control actions, and orchestration projection"
      min_lines: 150
      contains: "test_create_run"
  key_links:
    - from: "maf_core/control_plane/service.py"
      to: "maf_core/control_plane/store.py"
      via: "Run persistence and artifact manifest operations"
      pattern: "RunStore|save_run|load_run|artifact_manifest"
    - from: "maf_core/control_plane/service.py"
      to: "maf_core/orchestration.py"
      via: "Canonical stage, pause, and specialist state projection"
      pattern: "RunOrchestrationState|StageRecord|PauseKind"
    - from: "tests/test_phase6_service.py"
      to: "maf_core/control_plane/service.py"
      via: "Service contract validation for API-01 through API-04"
      pattern: "create_run|continue_run|approve|retry|cancel"
---

<objective>
Extract a shared control-plane package with canonical run contracts, durable persistence, and lifecycle service logic from `autogen_dashboard/` donors.

Purpose: Phase 6 cannot deliver a host-agnostic API until run ownership moves out of UI-specific code and into a shared runtime layer.
Output: A clean `maf_core/control_plane/` package with contracts, store, service, and regression coverage.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-api-boundary-and-control-plane-contract/06-CONTEXT.md
@.planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md
@autogen_dashboard/schemas.py
@autogen_dashboard/session_store.py
@autogen_dashboard/session_runner.py
@maf_core/orchestration.py
@maf_core/routing_policy.py
@maf_core/provider_fallback.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract canonical run contracts from autogen_dashboard/schemas.py</name>
  <files>maf_core/control_plane/contracts.py, maf_core/control_plane/__init__.py</files>
  <read_first>autogen_dashboard/schemas.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `maf_core/control_plane/__init__.py` as an empty package marker. Create `maf_core/control_plane/contracts.py` by extracting the core run contract models from `autogen_dashboard/schemas.py`: migrate `SessionSummary` as `RunSummary`, `SessionDetail` as `RunDetail`, `CreateSessionRequest` as `CreateRunRequest`, and related types like `WorkspaceInfo`, `RouteInfo`, `SpecialistInfo`, `ApprovalInfo`, `ValidationInfo`, `ArtifactInfo`, and `SessionEvent` as `RunEvent`. Preserve field names and structure but rename class references from `Session*` to `Run*`. Keep workspace, status, current_stage, pause_kind, routing, specialist_state, stage_timeline, artifacts, and transcript fields intact.</action>
  <verify>python -m compileall maf_core/control_plane</verify>
  <acceptance_criteria>
    - `maf_core/control_plane/contracts.py` contains `class RunSummary`
    - `maf_core/control_plane/contracts.py` contains `class RunDetail`
    - `maf_core/control_plane/contracts.py` contains `class CreateRunRequest`
    - `maf_core/control_plane/contracts.py` contains `class RunEvent`
    - `maf_core/control_plane/contracts.py` contains `workspace:`
    - `maf_core/control_plane/contracts.py` contains `current_stage`
    - `maf_core/control_plane/contracts.py` contains `routing`
    - `maf_core/control_plane/contracts.py` contains `artifacts`
    - `python -m compileall maf_core/control_plane` exits with code 0
  </acceptance_criteria>
  <done>Canonical run contracts are now in the shared control-plane package.</done>
</task>

<task type="auto">
  <name>Task 2: Extract durable run persistence from autogen_dashboard/session_store.py</name>
  <files>maf_core/control_plane/store.py</files>
  <read_first>autogen_dashboard/session_store.py, maf_core/control_plane/contracts.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `maf_core/control_plane/store.py` by extracting the `SessionStore` class from `autogen_dashboard/session_store.py` and renaming it to `RunStore`. Preserve the file-backed directory layout under `state/sessions/{run_id}/` for now. Migrate methods for saving/loading run metadata, runtime state, events, transcript, attempts, stage summaries, and artifact manifest. Keep `_build_artifact_manifest(...)` and manifest-relative artifact path logic intact. Update internal references from `session_id` to `run_id` and from `Session*` types to the new `Run*` types in `maf_core/control_plane/contracts.py`. Keep atomic JSON write patterns and directory creation logic unchanged.</action>
  <verify>python -m compileall maf_core/control_plane</verify>
  <acceptance_criteria>
    - `maf_core/control_plane/store.py` contains `class RunStore`
    - `maf_core/control_plane/store.py` contains `def save_run`
    - `maf_core/control_plane/store.py` contains `def load_run`
    - `maf_core/control_plane/store.py` contains `_build_artifact_manifest`
    - `maf_core/control_plane/store.py` contains `state/sessions`
    - `maf_core/control_plane/store.py` contains `run_id`
    - `python -m compileall maf_core/control_plane` exits with code 0
  </acceptance_criteria>
  <done>Durable run persistence is now shared and reusable by any host.</done>
</task>

<task type="auto">
  <name>Task 3: Extract run lifecycle and control service from autogen_dashboard/session_runner.py</name>
  <files>maf_core/control_plane/service.py</files>
  <read_first>autogen_dashboard/session_runner.py, maf_core/control_plane/store.py, maf_core/control_plane/contracts.py, maf_core/orchestration.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `maf_core/control_plane/service.py` with a `RunService` class that extracts the public lifecycle methods from `autogen_dashboard/session_runner.py`: `create_run`, `get_run`, `list_runs`, `continue_run`, `approve`, `retry`, `cancel`, and `append_operator_input`. Use the new `RunStore` for persistence and the new `Run*` contract types. Keep orchestration state projection logic that uses `RunOrchestrationState`, `StageRecord`, and pause semantics from `maf_core/orchestration.py`. Preserve workspace freshness checks, route metadata merging from `maf_core/routing_policy.py` and `maf_core/provider_fallback.py`, specialist state tracking, and event emission patterns. For now, stub out actual execution behind an internal `_execute_run(...)` placeholder method that raises NotImplementedError, since full execution engine extraction is deferred. Focus on control-plane API surface: create, status, control actions, and state projection.</action>
  <verify>python -m compileall maf_core/control_plane</verify>
  <acceptance_criteria>
    - `maf_core/control_plane/service.py` contains `class RunService`
    - `maf_core/control_plane/service.py` contains `def create_run`
    - `maf_core/control_plane/service.py` contains `def get_run`
    - `maf_core/control_plane/service.py` contains `def continue_run`
    - `maf_core/control_plane/service.py` contains `def approve`
    - `maf_core/control_plane/service.py` contains `def retry`
    - `maf_core/control_plane/service.py` contains `def cancel`
    - `maf_core/control_plane/service.py` contains `RunOrchestrationState`
    - `maf_core/control_plane/service.py` contains `RunStore`
    - `python -m compileall maf_core/control_plane` exits with code 0
  </acceptance_criteria>
  <done>Shared run service exposes create, status, and control actions ready for API-01 through API-04.</done>
</task>

<task type="auto">
  <name>Task 4: Add service-level validation tests for the shared control plane</name>
  <files>tests/test_phase6_service.py</files>
  <read_first>maf_core/control_plane/service.py, maf_core/control_plane/store.py, maf_core/control_plane/contracts.py, tests/test_run_persistence.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `tests/test_phase6_service.py` with unittest coverage for `RunService`. Use a temporary store directory under `.tmp-tests/phase6_service` following the pattern in `tests/test_run_persistence.py`. Add tests for: `test_create_run` validates that create returns a RunSummary with stable run_id, status, and workspace context (API-01). `test_get_run` validates status, current_stage, pause_kind, and route metadata are projected correctly (API-02). `test_control_actions` validates continue, approve, retry, and cancel update the same durable run (API-04). `test_artifact_manifest` validates artifact retrieval stays manifest-relative and never escapes the run directory (API-03). Run the test suite at task end.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase6_service -v</verify>
  <acceptance_criteria>
    - `tests/test_phase6_service.py` contains `test_create_run`
    - `tests/test_phase6_service.py` contains `test_get_run`
    - `tests/test_phase6_service.py` contains `test_control_actions`
    - `tests/test_phase6_service.py` contains `test_artifact_manifest`
    - `tests/test_phase6_service.py` contains `RunService`
    - `tests/test_phase6_service.py` contains `API-01`
    - `tests/test_phase6_service.py` contains `API-02`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase6_service -v` exits with code 0
  </acceptance_criteria>
  <done>Shared control-plane service is now regression-protected for core API contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase6_service -v`
- [ ] `python -m compileall maf_core/control_plane`
- [ ] Shared control-plane package exists under `maf_core/control_plane/`
- [ ] Run contracts, store, and service are extracted from `autogen_dashboard/` donors
- [ ] Service preserves durable run identity, workspace, stage, routing, specialist, and artifact semantics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared control-plane foundation ready for API router delivery in 06-02
  </success_criteria>

<output>
After completion, create `.planning/phases/06-api-boundary-and-control-plane-contract/06-01-SUMMARY.md`
</output>
