---
phase: 06-api-boundary-and-control-plane-contract
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - command_center/static/app.js
  - autogen_dashboard/app.py
  - tests/test_phase6_command_center_parity.py
  - README.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
user_setup: []
must_haves:
  truths:
    - "Command Center UI reads run data from the shared /api/v1 contract instead of parallel app-owned state."
    - "Legacy autogen_dashboard compatibility routes delegate to the shared control-plane service."
    - "The active product shell and external API observe the same run data and contract."
  artifacts:
    - path: "command_center/static/app.js"
      provides: "Command Center UI fetches run status from /api/v1 endpoints"
      min_lines: 800
      contains: "/api/v1/runs"
    - path: "autogen_dashboard/app.py"
      provides: "Legacy dashboard routes delegate to shared RunService"
      min_lines: 400
      contains: "RunService"
    - path: "tests/test_phase6_command_center_parity.py"
      provides: "Parity validation that Command Center and API share run contract"
      min_lines: 100
      contains: "test_command_center_api_parity"
    - path: "README.md"
      provides: "Documentation updated with /api/v1 endpoints and external API guidance"
      min_lines: 150
      contains: "/api/v1/runs"
  key_links:
    - from: "command_center/static/app.js"
      to: "maf_core/control_plane/router.py"
      via: "UI fetch calls to shared /api/v1 endpoints"
      pattern: "fetch|/api/v1/runs"
    - from: "autogen_dashboard/app.py"
      to: "maf_core/control_plane/service.py"
      via: "Legacy routes delegate to RunService"
      pattern: "RunService|session|compatibility"
    - from: "tests/test_phase6_command_center_parity.py"
      to: "command_center/app.py"
      via: "Validation that UI and API read same run state"
      pattern: "TestClient|/api/v1|run_id"
---

<objective>
Migrate Command Center UI onto the shared /api/v1 contract and align legacy dashboard routes as compatibility wrappers.

Purpose: Phase 6 cannot eliminate drift until the active product shell consumes the same run API that external callers use.
Output: Command Center reads from /api/v1, legacy dashboard delegates to RunService, parity tests, and updated documentation.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-api-boundary-and-control-plane-contract/06-CONTEXT.md
@.planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md
@command_center/static/app.js
@command_center/app.py
@autogen_dashboard/app.py
@maf_core/control_plane/router.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Command Center UI to fetch run data from /api/v1 endpoints</name>
  <files>command_center/static/app.js</files>
  <read_first>command_center/static/app.js, maf_core/control_plane/router.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>In `command_center/static/app.js`, update run-related fetch calls to use the new /api/v1 endpoints. Replace any direct backend calls that read run status, timeline, agents, routing, or artifacts with equivalent /api/v1 calls: use `GET /api/v1/runs` for run list, `GET /api/v1/runs/{run_id}` for run summary, `GET /api/v1/runs/{run_id}/timeline` for stage timeline, `GET /api/v1/runs/{run_id}/agents` for specialist states, `GET /api/v1/runs/{run_id}/routing` for route history, `GET /api/v1/runs/{run_id}/artifacts` for artifact manifest. Keep AG-UI streaming for live conversational transport but stop treating stream events as the only source of run state. Update run-status rendering, right-rail displays, and artifact links to use the shared API payloads.</action>
  <verify>node --check command_center\static\app.js</verify>
  <acceptance_criteria>
    - `command_center/static/app.js` contains `fetch("/api/v1/runs`
    - `command_center/static/app.js` contains `/api/v1/runs/${run_id}`
    - `command_center/static/app.js` contains `/timeline`
    - `command_center/static/app.js` contains `/agents`
    - `command_center/static/app.js` contains `/routing`
    - `command_center/static/app.js` contains `/artifacts`
    - `node --check command_center\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>Command Center now reads from the shared /api/v1 contract instead of parallel state.</done>
</task>

<task type="auto">
  <name>Task 2: Convert legacy dashboard routes into compatibility wrappers over RunService</name>
  <files>autogen_dashboard/app.py</files>
  <read_first>autogen_dashboard/app.py, maf_core/control_plane/service.py, maf_core/control_plane/contracts.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>In `autogen_dashboard/app.py`, import `RunService` from `maf_core.control_plane.service` and `Run*` types from `maf_core.control_plane.contracts`. Update existing session endpoints to delegate to the shared service: `/api/sessions` POST becomes a wrapper around `RunService.create_run()` that maps `CreateSessionRequest` to `CreateRunRequest` and returns a session-shaped response. `/api/sessions/{session_id}` GET delegates to `RunService.get_run(run_id)` and maps `RunDetail` back to `SessionDetail` format. `/api/sessions/{session_id}/retry`, `/approve`, `/run`, `/cancel` delegate to corresponding `RunService` methods. Keep SSE event streaming for compatibility but note in code comments that the dashboard is now a compatibility shell. Add a deprecation comment at the top of `autogen_dashboard/app.py` noting that this surface is preserved for backward compatibility only and new work should use /api/v1.</action>
  <verify>python -m compileall autogen_dashboard</verify>
  <acceptance_criteria>
    - `autogen_dashboard/app.py` contains `from maf_core.control_plane.service import RunService`
    - `autogen_dashboard/app.py` contains `RunService.create_run`
    - `autogen_dashboard/app.py` contains `RunService.get_run`
    - `autogen_dashboard/app.py` contains `RunService.retry`
    - `autogen_dashboard/app.py` contains `compatibility`
    - `autogen_dashboard/app.py` contains `deprecated`
    - `python -m compileall autogen_dashboard` exits with code 0
  </acceptance_criteria>
  <done>Legacy dashboard now delegates to the shared service instead of owning parallel run logic.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity tests validating Command Center and API share run contract</name>
  <files>tests/test_phase6_command_center_parity.py</files>
  <read_first>command_center/app.py, maf_core/control_plane/router.py, tests/test_command_center.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-RESEARCH.md</read_first>
  <action>Create `tests/test_phase6_command_center_parity.py` using `fastapi.testclient.TestClient` with `create_command_center_app(...)`. Add `test_command_center_api_parity` that creates a run via POST /api/v1/runs, fetches it via GET /api/v1/runs/{run_id}, then confirms Command Center status endpoints or UI-facing helpers return the same run_id, status, current_stage, and workspace data. Add `test_artifact_manifest_parity` that validates artifact links from /api/v1/runs/{run_id}/artifacts match what Command Center UI would render. Add `test_legacy_dashboard_delegation` that calls a legacy `/api/sessions` endpoint and confirms it returns data from the same underlying RunService. Run the test suite at task end.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase6_command_center_parity -v</verify>
  <acceptance_criteria>
    - `tests/test_phase6_command_center_parity.py` contains `test_command_center_api_parity`
    - `tests/test_phase6_command_center_parity.py` contains `test_artifact_manifest_parity`
    - `tests/test_phase6_command_center_parity.py` contains `test_legacy_dashboard_delegation`
    - `tests/test_phase6_command_center_parity.py` contains `/api/v1/runs`
    - `tests/test_phase6_command_center_parity.py` contains `RunService`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_phase6_command_center_parity -v` exits with code 0
  </acceptance_criteria>
  <done>Parity between Command Center, /api/v1, and legacy compatibility is now validated.</done>
</task>

<task type="auto">
  <name>Task 4: Update README.md with /api/v1 endpoints and external API guidance</name>
  <files>README.md</files>
  <read_first>README.md, maf_core/control_plane/router.py, .planning/phases/06-api-boundary-and-control-plane-contract/06-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update `README.md` to document the new `/api/v1` control-plane API. Add a new section titled "## External API" after the existing Command Center and DevUI sections. Document the key endpoints: `POST /api/v1/runs` (create a new run), `GET /api/v1/runs/{run_id}` (get run status and summary), `GET /api/v1/runs/{run_id}/timeline` (stage timeline), `GET /api/v1/runs/{run_id}/agents` (specialist states), `GET /api/v1/runs/{run_id}/routing` (route history), `GET /api/v1/runs/{run_id}/artifacts` (artifact manifest), `POST /api/v1/runs/{run_id}/actions/*` (control actions). Note that the API is host-agnostic and ready for Azure Functions hosting in Phase 7. Mention that Command Center and external callers use the same contract. Update the "What's next" or future work section to reference Phase 7 (Azure Functions) and Phase 8 (background worker boundary).</action>
  <verify>python -c "open('README.md').read()"</verify>
  <acceptance_criteria>
    - `README.md` contains `## External API`
    - `README.md` contains `/api/v1/runs`
    - `README.md` contains `POST /api/v1/runs`
    - `README.md` contains `GET /api/v1/runs/{run_id}`
    - `README.md` contains `timeline`
    - `README.md` contains `agents`
    - `README.md` contains `routing`
    - `README.md` contains `artifacts`
    - `README.md` contains `Phase 7`
    - `README.md` contains `Azure Functions`
  </acceptance_criteria>
  <done>README now documents the external API and positions Phase 6 as the control-plane foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase6_command_center_parity -v`
- [ ] `node --check command_center\static\app.js`
- [ ] `python -m compileall autogen_dashboard command_center`
- [ ] Command Center UI fetches run data from /api/v1 endpoints
- [ ] Legacy dashboard delegates to RunService instead of owning parallel logic
- [ ] README documents /api/v1 endpoints and external API guidance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Command Center and external API share the same run contract
- Phase 6 drift eliminated: product shell and API are aligned
  </success_criteria>

<output>
After completion, create `.planning/phases/06-api-boundary-and-control-plane-contract/06-03-SUMMARY.md`
</output>
