---
phase: 01-workspace-and-durable-run-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - autogen_dashboard/session_store.py
  - autogen_dashboard/session_runner.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/dependencies.py
  - tests/test_run_persistence.py
  - tests/test_phase1_api.py
  - tests/test_phase1_runtime.py
autonomous: true
requirements:
  - WKSP-03
user_setup: []
must_haves:
  truths:
    - "Each run has one stable run ID and a durable directory that survives pause, resume, retry, and reopen."
    - "Retry uses the original engineering task or explicit retry seed, not the last approval or rejection note."
    - "A reopened run exposes prior transcript, events, artifacts, and attempt history."
  artifacts:
    - path: "autogen_dashboard/session_store.py"
      provides: "Stable run directory layout with metadata, transcript, events, artifacts, and runtime state"
      min_lines: 90
      contains: "metadata.json"
    - path: "autogen_dashboard/session_runner.py"
      provides: "Explicit run lifecycle and retry semantics"
      min_lines: 250
      contains: "original_task"
    - path: "tests/test_run_persistence.py"
      provides: "Regression coverage for run persistence and retry semantics"
      min_lines: 60
  key_links:
    - from: "autogen_dashboard/session_runner.py"
      to: "autogen_dashboard/session_store.py"
      via: "run directory and attempt persistence"
      pattern: "save_summary|save_state|append_event"
    - from: "autogen_dashboard/session_runner.py"
      to: "autogen_dashboard/schemas.py"
      via: "explicit run fields and status model"
      pattern: "original_task|latest_human_note|attempt"
---

<objective>
Unify durable run state so resume, reopen, and retry behave like one coherent run history instead of loosely connected chat turns.

Purpose: Durable orchestration depends on one stable run contract with explicit attempts, separate human decisions, and preserved artifacts.
Output: A run directory contract, explicit attempt metadata, and regression tests that lock retry and reopen semantics.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-durable-run-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-and-durable-run-foundation/01-RESEARCH.md
@autogen_dashboard/session_store.py
@autogen_dashboard/session_runner.py
@autogen_dashboard/schemas.py
@autogen_dashboard/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the on-disk run directory and artifact manifest</name>
  <files>autogen_dashboard/session_store.py, autogen_dashboard/schemas.py, autogen_dashboard/dependencies.py</files>
  <read_first>autogen_dashboard/session_store.py, autogen_dashboard/schemas.py, autogen_dashboard/dependencies.py, .planning/phases/01-workspace-and-durable-run-foundation/01-RESEARCH.md</read_first>
  <action>Refactor the existing session storage into a run-shaped directory contract under `state/sessions/<run-id>/`. Keep `metadata.json`, `transcript.json`, and `events.jsonl`, and add dedicated folders or files for `artifacts/manifest.json`, `artifacts/workspace/creation.json`, `runtime/`, and `attempts/attempt-001/summary.json`. Mutable JSON writes must stay atomic. Extend the schema so summary/detail responses include `attempt_count`, `artifact_manifest`, `latest_attempt_id`, and whether checkpoint state exists under the runtime directory instead of one loose state file.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_run_persistence</verify>
  <acceptance_criteria>
    - `autogen_dashboard/session_store.py` contains `artifacts`
    - `autogen_dashboard/session_store.py` contains `attempts`
    - `autogen_dashboard/schemas.py` contains `artifact_manifest`
    - `autogen_dashboard/schemas.py` contains `latest_attempt_id`
    - `tests/test_run_persistence.py` exits with code 0
  </acceptance_criteria>
  <done>The run directory contract explicitly stores runtime state, artifacts, and attempts under one stable run ID.</done>
</task>

<task type="auto">
  <name>Task 2: Separate original task, human decisions, and retry seeds in the run lifecycle</name>
  <files>autogen_dashboard/session_runner.py, autogen_dashboard/session_store.py, autogen_dashboard/schemas.py</files>
  <read_first>autogen_dashboard/session_runner.py, autogen_dashboard/session_store.py, autogen_dashboard/schemas.py, .planning/phases/01-workspace-and-durable-run-foundation/01-CONTEXT.md</read_first>
  <action>Update the runner so `original_task`, `latest_human_note`, `approval_decisions`, and `retry_seed_prompt` are separate persisted fields. `approve` and `reject` must still append transcript or event entries, but they must never overwrite `original_task`. `retry()` must start a new attempt under the same run using `retry_seed_prompt` or `original_task`, increment `attempt_count`, and emit explicit events such as `run.attempt.started`, `run.attempt.completed`, and `run.attempt.failed`. Keep operator-visible statuses explicit: `queued`, `running`, `waiting`, `completed`, `error`, and `stopped`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase1_runtime</verify>
  <acceptance_criteria>
    - `autogen_dashboard/session_runner.py` contains `original_task`
    - `autogen_dashboard/session_runner.py` contains `retry_seed_prompt`
    - `autogen_dashboard/session_runner.py` contains `run.attempt.started`
    - `autogen_dashboard/session_runner.py` contains `run.attempt.completed`
    - `tests/test_phase1_runtime.py` exits with code 0
  </acceptance_criteria>
  <done>Resume, reopen, and retry preserve run identity and no longer collapse into approval-token retries.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence and reopen regression coverage</name>
  <files>tests/test_run_persistence.py, tests/test_phase1_api.py, tests/test_phase1_runtime.py</files>
  <read_first>tests/test_maf_setup.py, autogen_dashboard/session_store.py, autogen_dashboard/session_runner.py, autogen_dashboard/app.py</read_first>
  <action>Create focused `unittest` coverage for the new run contract. `tests/test_run_persistence.py` must verify the on-disk layout, atomic JSON writes, artifact manifest creation, and attempt directory creation. `tests/test_phase1_api.py` must verify the operator API contract for create/get/retry/reopen flows, including that create-run rejects an empty `task` and returns persisted workspace and attempt metadata. `tests/test_phase1_runtime.py` must verify create -> run -> approve/reject -> retry -> reopen behavior, including that approval notes do not replace the original task and that a reopened run returns prior transcript, events, and artifacts.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_run_persistence tests.test_phase1_api tests.test_phase1_runtime</verify>
  <acceptance_criteria>
    - `tests/test_run_persistence.py` contains `artifact_manifest`
    - `tests/test_phase1_api.py` contains `task`
    - `tests/test_phase1_api.py` contains `retry`
    - `tests/test_phase1_runtime.py` contains `retry`
    - `tests/test_phase1_runtime.py` contains `original_task`
    - `.\.venv\Scripts\python.exe -m unittest tests.test_run_persistence tests.test_phase1_api tests.test_phase1_runtime` exits with code 0
  </acceptance_criteria>
  <done>The durable run contract is protected by targeted regression tests instead of only manual checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_run_persistence tests.test_phase1_api tests.test_phase1_runtime`
- [ ] Run metadata, transcript, events, artifacts, and attempts live under one run directory
- [ ] Retry never reuses `APPROVE` or `REJECT` as the primary task unless explicitly set as a retry seed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Durable run identity, reopen, and retry semantics satisfy `WKSP-03`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-durable-run-foundation/01-02-SUMMARY.md`
</output>
