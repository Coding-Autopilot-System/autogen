---
phase: 01-workspace-and-durable-run-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - maf_starter/config.py
  - maf_starter/agent_factory.py
  - maf_starter/tools.py
  - maf_starter/workflow_factory.py
  - maf_starter/team_factory.py
  - maf_starter/provider_fallback.py
  - autogen_dashboard/session_runner.py
  - autogen_dashboard/app.py
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/app.js
  - autogen_dashboard/static/styles.css
  - tests/test_maf_setup.py
  - tests/test_phase1_runtime.py
autonomous: true
requirements:
  - WKSP-01
  - WKSP-02
  - WKSP-03
user_setup: []
must_haves:
  truths:
    - "The active runtime uses the selected run workspace instead of only one startup-time `MAF_REPO_ROOT`."
    - "Each run refreshes and persists workspace snapshots at lifecycle boundaries and flags stale workspace drift visibly."
    - "The operator surface shows workspace context, current attempt, and stale or fresh state without reading raw event logs."
  artifacts:
    - path: "maf_starter/config.py"
      provides: "Run-scoped workspace and checkpoint override helpers"
      min_lines: 80
      contains: "checkpoint_dir"
    - path: "autogen_dashboard/session_runner.py"
      provides: "Workspace snapshot refresh and stale-workspace events"
      min_lines: 280
      contains: "workspace.stale"
    - path: "tests/test_phase1_runtime.py"
      provides: "Integration coverage for run-scoped workspace and stale warnings"
      min_lines: 80
  key_links:
    - from: "maf_starter/agent_factory.py"
      to: "maf_starter/tools.py"
      via: "run-scoped repo tool binding"
      pattern: "build_repo_tools"
    - from: "maf_starter/provider_fallback.py"
      to: "autogen_dashboard/session_runner.py"
      via: "CLI fallback working directory and run-scoped workspace propagation"
      pattern: "working_directory|repo_root"
    - from: "autogen_dashboard/static/app.js"
      to: "autogen_dashboard/session_runner.py"
      via: "workspace stale banner and attempt metadata rendering"
      pattern: "stale|attempt"
---

<objective>
Thread the selected workspace and workspace freshness through the active MAF runtime, traces, and operator surface.

Purpose: Phase 1 is incomplete if workspace selection exists only in a legacy shell while the active runtime still executes against one static repo root with no freshness model.
Output: Run-scoped workspace overrides for the MAF runtime plus visible workspace and stale-state surfaces in the operator UI.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-durable-run-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-and-durable-run-foundation/01-RESEARCH.md
@maf_starter/config.py
@maf_starter/agent_factory.py
@maf_starter/tools.py
@maf_starter/workflow_factory.py
@maf_starter/team_factory.py
@maf_starter/provider_fallback.py
@autogen_dashboard/session_runner.py
@autogen_dashboard/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the active MAF runtime run-scoped instead of startup-root scoped</name>
  <files>maf_starter/config.py, maf_starter/agent_factory.py, maf_starter/tools.py, maf_starter/workflow_factory.py, maf_starter/team_factory.py, maf_starter/provider_fallback.py</files>
  <read_first>maf_starter/config.py, maf_starter/agent_factory.py, maf_starter/tools.py, maf_starter/workflow_factory.py, maf_starter/team_factory.py, maf_starter/provider_fallback.py, .planning/phases/01-workspace-and-durable-run-foundation/01-RESEARCH.md</read_first>
  <action>Add a run-scoped settings override path so the selected workspace root and checkpoint directory can be derived per run instead of only from `MAF_REPO_ROOT`. Introduce a helper such as `with_repo_root(...)/with_checkpoint_dir(...)` or an equivalent immutable settings clone. Bind repo tools, CLI fallback working directories, and checkpoint storage to the selected run workspace and `state/sessions/<run-id>/runtime/` path. Keep the existing top-level env vars as defaults, but they must no longer be the only execution target.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_phase1_runtime</verify>
  <acceptance_criteria>
    - `maf_starter/config.py` contains a helper for run-scoped repo or checkpoint overrides
    - `maf_starter/tools.py` still builds tools from an explicit repo root argument
    - `maf_starter/provider_fallback.py` uses the run-selected working directory instead of only `settings.repo_root`
    - `maf_starter/workflow_factory.py` builds checkpoint storage from a run-specific path
    - `tests/test_phase1_runtime.py` exits with code 0
  </acceptance_criteria>
  <done>The active runtime can execute against the selected run workspace and persist checkpoints under the run directory.</done>
</task>

<task type="auto">
  <name>Task 2: Add workspace snapshot refresh, stale warnings, and operator visibility</name>
  <files>autogen_dashboard/session_runner.py, autogen_dashboard/app.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/session_runner.py, autogen_dashboard/app.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, autogen_dashboard/repo_context.py</read_first>
  <action>Refresh the persisted workspace snapshot at create, run start, resume, retry, and major stage boundaries. Compare the new snapshot with the last saved snapshot using repo root, branch, dirty flag, and changed-file signature; when they differ, emit a structured `workspace.stale` event and mark the run summary with a stale-warning flag and human-readable detail. Expose this state through the API and render it in the operator surface as a rounded warning banner near the run header, alongside current workspace summary and attempt metadata.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/session_runner.py` contains `workspace.stale`
    - `autogen_dashboard/session_runner.py` contains a snapshot refresh call in run or retry boundaries
    - `autogen_dashboard/static/app.js` contains a stale-workspace rendering branch
    - `autogen_dashboard/static/styles.css` contains a dedicated stale-warning style block
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The operator can see current workspace context and stale-workspace warnings without opening raw logs.</done>
</task>

<task type="auto">
  <name>Task 3: Lock run-scoped workspace behavior with integration tests and smoke checks</name>
  <files>tests/test_phase1_runtime.py, tests/test_maf_setup.py</files>
  <read_first>tests/test_phase1_runtime.py, tests/test_maf_setup.py, maf_starter/config.py, autogen_dashboard/session_runner.py</read_first>
  <action>Extend the test suite to verify run-scoped repo-root overrides, run-specific checkpoint locations, workspace snapshot refresh on resume and retry, and stale-warning emission when the underlying repo changes between snapshots. Keep the smoke baseline in `tests/test_maf_setup.py` and add focused Phase 1 integration coverage in `tests/test_phase1_runtime.py`. End the plan by running the full suite and a compile smoke across `maf_starter`, `autogen_dashboard`, `tests`, and `main.py`.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `tests/test_phase1_runtime.py` contains `workspace.stale`
    - `tests/test_phase1_runtime.py` contains run-scoped checkpoint assertions
    - `tests/test_maf_setup.py` still exits with code 0
    - `.\.venv\Scripts\python.exe -m unittest discover -s tests -v` exits with code 0
  </acceptance_criteria>
  <done>The selected workspace, runtime handoff, and stale detection behavior are covered by repeatable automated checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_phase1_runtime`
- [ ] `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`
- [ ] `.\.venv\Scripts\python.exe -m compileall maf_starter autogen_dashboard tests main.py`
- [ ] The selected workspace flows from run creation into repo tools, CLI fallback working directories, and checkpoint storage
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Workspace visibility and freshness are consistent across UI, runtime, and traces, completing `WKSP-01`, `WKSP-02`, and `WKSP-03`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-durable-run-foundation/01-03-SUMMARY.md`
</output>
