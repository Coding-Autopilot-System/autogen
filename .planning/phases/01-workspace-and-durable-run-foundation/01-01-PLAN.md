---
phase: 01-workspace-and-durable-run-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - autogen_dashboard/repo_context.py
  - autogen_dashboard/schemas.py
  - autogen_dashboard/app.py
  - autogen_dashboard/static/index.html
  - autogen_dashboard/static/app.js
  - autogen_dashboard/static/styles.css
  - tests/test_workspace_contract.py
autonomous: true
requirements:
  - WKSP-01
  - WKSP-02
user_setup: []
must_haves:
  truths:
    - "An operator can create a run by selecting an explicit repo or worktree and entering one engineering prompt."
    - "An operator can create a run only after selecting an explicit repo or worktree inside the allowed scan root."
    - "The run-creation surface shows repo root, branch, dirty state, stack hints, and recent commits before the first execution."
  artifacts:
    - path: "autogen_dashboard/repo_context.py"
      provides: "Recursive workspace discovery and explicit repo resolution inside the configured scan root"
      min_lines: 80
      contains: "discover_local_repos"
    - path: "autogen_dashboard/schemas.py"
      provides: "Run or workspace schema fields for selected workspace, workspace snapshot, and explicit run title"
      min_lines: 80
      contains: "class RepoContext"
    - path: "tests/test_workspace_contract.py"
      provides: "Regression coverage for repo selection and workspace snapshot creation"
      min_lines: 40
  key_links:
    - from: "autogen_dashboard/app.py"
      to: "autogen_dashboard/repo_context.py"
      via: "repo list and create-run validation"
      pattern: "available_repos|resolve_repo_root"
    - from: "autogen_dashboard/static/app.js"
      to: "/api/repos"
      via: "workspace picker population"
      pattern: "/api/repos"
---

<objective>
Establish the explicit workspace-selection and run-creation contract at the operator boundary.

Purpose: Later orchestration work is unsafe if runs still depend on one hidden global repo root or ambiguous session creation semantics.
Output: A run-creation flow backed by explicit workspace metadata, recursive repo discovery, and a first-class run schema.
</objective>

<execution_context>
@C:/codex-home/get-shit-done/workflows/execute-plan.md
@C:/codex-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-durable-run-foundation/01-CONTEXT.md
@.planning/phases/01-workspace-and-durable-run-foundation/01-RESEARCH.md
@autogen_dashboard/repo_context.py
@autogen_dashboard/schemas.py
@autogen_dashboard/app.py
@autogen_dashboard/static/index.html
@autogen_dashboard/static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand workspace discovery and explicit run schema</name>
  <files>autogen_dashboard/repo_context.py, autogen_dashboard/schemas.py</files>
  <read_first>autogen_dashboard/repo_context.py, autogen_dashboard/schemas.py, .planning/phases/01-workspace-and-durable-run-foundation/01-CONTEXT.md</read_first>
  <action>Refactor `discover_local_repos` to scan recursively under `AUTOGEN_REPO_SCAN_ROOT` instead of only iterating immediate children, while still ignoring `.git`, `.venv`, `state`, and `.tmp-tests`. Keep `resolve_repo_root` strict: absolute and manual paths must resolve inside the scan root, must be directories, and must be git repositories. Extend the schema layer so run creation carries explicit workspace data: add `workspace_snapshot`, `workspace_kind`, `original_task`, `latest_human_note`, `attempt_count`, and a durable operator-facing `title`. Preserve current `Session*` payload names where needed, but the values must express run semantics instead of loose chat-session semantics.</action>
  <verify>.\.venv\Scripts\python.exe -m unittest tests.test_workspace_contract</verify>
  <acceptance_criteria>
    - `autogen_dashboard/repo_context.py` contains a recursive implementation for `discover_local_repos`
    - `autogen_dashboard/repo_context.py` still contains `resolve_repo_root`
    - `autogen_dashboard/schemas.py` contains `workspace_snapshot`
    - `autogen_dashboard/schemas.py` contains `original_task`
    - `tests/test_workspace_contract.py` exits with code 0
  </acceptance_criteria>
  <done>The repo scan and schema contract support explicit workspace selection and run creation inside the configured root.</done>
</task>

<task type="auto">
  <name>Task 2: Require explicit workspace selection in the operator surface</name>
  <files>autogen_dashboard/app.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css</files>
  <read_first>autogen_dashboard/app.py, autogen_dashboard/static/index.html, autogen_dashboard/static/app.js, autogen_dashboard/static/styles.css, autogen_dashboard/repo_context.py</read_first>
  <action>Update the local operator UI and API contract so run creation always goes through a visible workspace picker backed by `/api/repos`, with optional manual path entry only when the path still resolves inside the scan root. The create-run surface must require both a selected workspace and a non-empty engineering prompt before submit. Keep the existing `/api/sessions` route if that is the least disruptive API seam, but the handler must reject empty `task` values with a 400-level validation error and the UI copy and payload handling must treat it as a run-creation flow. The create-run surface must show repo name, absolute root, branch, dirty/clean badge, stack hints, and the three most recent commits before submit. Style the picker and workspace summary as rounded cards matching the repo's existing polished dashboard direction.</action>
  <verify>node --check autogen_dashboard\static\app.js</verify>
  <acceptance_criteria>
    - `autogen_dashboard/app.py` contains `/api/repos`
    - `autogen_dashboard/static/index.html` contains a workspace or repo selector control
    - `autogen_dashboard/static/index.html` contains a prompt input control for the engineering task
    - `autogen_dashboard/static/app.js` fetches `/api/repos`
    - `autogen_dashboard/app.py` rejects empty `task` input for create-run requests
    - `autogen_dashboard/static/styles.css` contains a dedicated workspace summary style block
    - `node --check autogen_dashboard\static\app.js` exits with code 0
  </acceptance_criteria>
  <done>The operator can only start a run from an explicit workspace selection plus a non-empty engineering prompt and sees the workspace summary before execution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\.venv\Scripts\python.exe -m unittest tests.test_workspace_contract`
- [ ] `node --check autogen_dashboard\static\app.js`
- [ ] Run creation requires a selected workspace, a non-empty engineering prompt, and no hidden global repo root remains in the UI flow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Explicit workspace selection plus prompt entry satisfies `WKSP-01` and exposes summary data for `WKSP-02`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-durable-run-foundation/01-01-SUMMARY.md`
</output>
